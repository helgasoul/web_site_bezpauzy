# Следующие шаги после настройки .env.local

## ✅ Что вы уже сделали:
- Создали файл `.env.local`
- Внесли данные Supabase (URL и Anon Key)

## 🔍 Шаг 1: Проверка переменных окружения

Убедитесь, что в `.env.local` есть обе переменные:

```env
NEXT_PUBLIC_SUPABASE_URL=https://your-supabase-url.com
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here
```

⚠️ **Важно:**
- Переменные должны начинаться с `NEXT_PUBLIC_` (иначе они не будут доступны в браузере)
- Не должно быть пробелов вокруг `=`
- Не используйте кавычки вокруг значений

## 🗄️ Шаг 2: Выполнение SQL миграции

Теперь нужно создать таблицу в Supabase:

1. **Откройте Supabase Dashboard** на вашем Beget сервере
   - Обычно доступен по адресу вашего домена

2. **Перейдите в SQL Editor**

3. **Откройте файл** `supabase/migrations/001_create_community_members.sql`

4. **Скопируйте весь SQL код** из файла

5. **Вставьте в SQL Editor** и нажмите **Run** (или `Cmd/Ctrl + Enter`)

6. **Проверьте результат** - должно быть сообщение об успехе:
   ```
   Success. No rows returned
   ```

## 🧪 Шаг 3: Проверка подключения

1. **Перезапустите dev-сервер** (если он уже запущен):
   ```bash
   # Остановите сервер (Ctrl+C)
   # Затем запустите снова
   npm run dev
   ```

2. **Откройте браузер** и перейдите на:
   ```
   http://localhost:3000/community
   ```

3. **Нажмите кнопку** "Присоединиться к сообществу"

4. **Заполните форму** и отправьте

5. **Проверьте в Supabase Dashboard** → **Table Editor** → `menohub_community_members`:
   - Должна появиться новая запись с вашими данными

## ❌ Если что-то не работает

### Ошибка: "Missing Supabase environment variables"

**Решение:**
- Убедитесь, что файл называется именно `.env.local` (с точкой в начале)
- Проверьте, что переменные названы правильно
- Перезапустите dev-сервер после изменения `.env.local`

### Ошибка: "relation does not exist"

**Решение:**
- Убедитесь, что SQL миграция выполнена успешно
- Проверьте название таблицы: должно быть `menohub_community_members`

### Ошибка подключения к Supabase

**Решение:**
- Проверьте, что URL правильный (без лишних пробелов)
- Проверьте, что Supabase доступен по указанному адресу
- Проверьте интернет-соединение

## 📝 Проверка структуры .env.local

Ваш файл `.env.local` должен выглядеть примерно так:

```env
NEXT_PUBLIC_SUPABASE_URL=https://supabase.yourdomain.com
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6ImFiY2RlZmdoaWprbG1ub3AiLCJyb2xlIjoiYW5vbiIsImlhdCI6MTYzODk2ODAwMCwiZXhwIjoxOTU0NTQ0MDAwfQ.example
```

**НЕ должно быть:**
- Кавычек вокруг значений
- Пробелов вокруг `=`
- Комментариев на той же строке (используйте отдельные строки)

## ✅ Готово!

После выполнения всех шагов форма регистрации в сообщество должна работать. Данные будут сохраняться в таблице `menohub_community_members` в вашей базе данных Supabase на Beget.

