# Настройка Upstash Redis для Rate Limiting

## 📋 Что такое Upstash?

Upstash - это serverless Redis, идеально подходящий для Next.js и serverless функций:
- ✅ Работает в serverless окружении (Vercel, AWS Lambda)
- ✅ Pay-per-use модель (бесплатный tier: 10,000 команд/день)
- ✅ Нет необходимости управлять сервером
- ✅ Автоматическое масштабирование
- ✅ Глобальное распределение

## 🚀 Шаг 1: Создание аккаунта и Redis базы

1. Перейдите на [console.upstash.com](https://console.upstash.com/)
2. Зарегистрируйтесь или войдите
3. Нажмите **"Create Database"**
4. Выберите:
   - **Type:** Regional (для лучшей производительности) или Global (для глобального распределения)
   - **Region:** Выберите ближайший регион (например, `eu-west-1` для Европы)
   - **Name:** `bezpauzy-rate-limit` (или любое другое имя)
5. Нажмите **"Create"**

## 🔑 Шаг 2: Получение ключей

После создания базы данных:

1. Откройте созданную базу данных
2. Перейдите на вкладку **"REST API"**
3. Скопируйте:
   - **UPSTASH_REDIS_REST_URL** (например: `https://xxx.upstash.io`)
   - **UPSTASH_REDIS_REST_TOKEN** (длинная строка токена)

## 📦 Шаг 3: Установка зависимостей

```bash
npm install @upstash/ratelimit @upstash/redis
```

## 🔐 Шаг 4: Настройка переменных окружения

Добавьте в файл `.env.local`:

```env
# Upstash Redis для Rate Limiting
UPSTASH_REDIS_REST_URL=https://your-redis-url.upstash.io
UPSTASH_REDIS_REST_TOKEN=your-redis-token-here
```

**⚠️ ВАЖНО:**
- Никогда не коммитьте `.env.local` в git (уже в `.gitignore`)
- Для production добавьте эти переменные в настройки Vercel/хостинга

## ✅ Шаг 5: Проверка работы

После установки зависимостей и добавления переменных окружения, rate limiting автоматически переключится на Upstash Redis.

Проверить можно:
1. Запустить dev сервер: `npm run dev`
2. Отправить несколько запросов к API
3. Проверить в Upstash Console → Database → Data - должны появиться ключи

## 💰 Стоимость

**Free Tier:**
- 10,000 команд Redis в день
- Достаточно для ~100,000 rate limit проверок в день
- Идеально для MVP и небольших проектов

**Paid Plans:**
- Pay-as-you-go: $0.20 за 100,000 команд
- Очень дешево для production

## 🔄 Миграция с in-memory на Upstash

Код автоматически определяет наличие переменных окружения:
- Если `UPSTASH_REDIS_REST_URL` и `UPSTASH_REDIS_REST_TOKEN` установлены → использует Upstash
- Если нет → использует in-memory (fallback)

**Никаких изменений в коде не требуется!**

## 🧪 Тестирование

После настройки проверьте работу:

```bash
# Отправить несколько запросов подряд
for i in {1..6}; do
  curl -X POST http://localhost:3000/api/auth/telegram/verify-code \
    -H "Content-Type: application/json" \
    -d '{"code":"123456"}'
  echo ""
done
```

6-й запрос должен вернуть `429 Too Many Requests`.

## 📊 Мониторинг

В Upstash Console можно:
- Видеть количество команд в реальном времени
- Просматривать данные в Redis
- Настроить алерты при превышении лимитов

## 🔧 Troubleshooting

### Проблема: Rate limiting не работает

**Решение:**
1. Проверьте, что переменные окружения установлены: `echo $UPSTASH_REDIS_REST_URL`
2. Проверьте формат URL (должен начинаться с `https://`)
3. Проверьте токен (должен быть длинной строкой)
4. Перезапустите dev сервер после добавления переменных

### Проблема: Ошибка подключения к Redis

**Решение:**
1. Проверьте интернет соединение
2. Убедитесь, что URL и токен правильные
3. Проверьте, что база данных активна в Upstash Console
4. Попробуйте создать новую базу данных

### Проблема: Превышен free tier

**Решение:**
1. Проверьте использование в Upstash Console
2. Оптимизируйте rate limit конфигурации
3. Рассмотрите upgrade на paid plan

## 📚 Дополнительные ресурсы

- [Upstash Documentation](https://upstash.com/docs)
- [@upstash/ratelimit GitHub](https://github.com/upstash/ratelimit)
- [Next.js Rate Limiting Guide](https://upstash.com/blog/nextjs-ratelimiting)

---

**Последнее обновление:** 2025-01-05

