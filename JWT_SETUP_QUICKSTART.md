# 🚀 Быстрый старт: JWT сессии

## Шаг 1: Установить зависимости (1 минута)

```bash
npm install jsonwebtoken
npm install --save-dev @types/jsonwebtoken
```

## Шаг 2: Добавить JWT_SECRET в .env.local (1 минута)

Откройте `.env.local` и добавьте:

```env
JWT_SECRET=your-super-secret-key-minimum-32-characters-long
```

**Генерация безопасного ключа:**
```bash
openssl rand -base64 32
```

Или используйте онлайн генератор: https://www.lastpass.com/features/password-generator

## ✅ Готово!

После установки зависимостей и добавления `JWT_SECRET`:
- ✅ Все новые сессии будут использовать безопасные JWT токены
- ✅ Старые base64 токены продолжат работать (обратная совместимость)
- ✅ При следующем входе пользователи получат новые JWT токены

## 📚 Подробная документация

См. `docs/JWT_SESSION_SETUP.md` для детальной информации.

