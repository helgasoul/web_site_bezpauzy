# Rate Limiting - Документация

## 📋 Обзор

Rate Limiting защищает API от злоупотреблений, DDoS атак и brute-force попыток, ограничивая количество запросов от одного источника за определенный период времени.

## 🔧 Реализация

### Текущая реализация

Используется **in-memory rate limiter** (хранит данные в памяти сервера).

**Преимущества:**
- ✅ Не требует внешних зависимостей
- ✅ Быстрая работа
- ✅ Простая настройка

**Ограничения:**
- ⚠️ Данные сбрасываются при перезапуске сервера
- ⚠️ Не работает в multi-instance окружении (несколько серверов)
- ⚠️ Для production лучше использовать Redis-based решение

### Конфигурации лимитов

```typescript
// lib/rate-limit.ts

RateLimitConfigs = {
  // Строгие лимиты для аутентификации
  auth: {
    interval: 15 * 60 * 1000, // 15 минут
    limit: 5,                  // 5 запросов за 15 минут
  },
  
  // Умеренные лимиты для подписок
  subscription: {
    interval: 60 * 60 * 1000, // 1 час
    limit: 3,                  // 3 запроса за час
  },
  
  // Более мягкие лимиты для общего API
  api: {
    interval: 60 * 1000,       // 1 минута
    limit: 60,                 // 60 запросов за минуту
  },
  
  // Очень строгие лимиты для чувствительных операций
  sensitive: {
    interval: 60 * 60 * 1000, // 1 час
    limit: 10,                 // 10 запросов за час
  },
}
```

## 🎯 Применение

Rate limiting автоматически применяется в `middleware.ts` к следующим типам endpoints:

### 1. Аутентификация (`/api/auth/*`)
- **Лимит:** 5 запросов за 15 минут
- **Защищает от:** Brute-force атак на логин/регистрацию

### 2. Подписки (`/api/newsletter/subscribe`, `/api/community/join`)
- **Лимит:** 3 запроса за час
- **Защищает от:** Спама и множественных подписок

### 3. Чувствительные операции (`/api/quiz/save-results`, `/api/account/*`)
- **Лимит:** 10 запросов за час
- **Защищает от:** Злоупотребления сохранением данных

### 4. Общие API endpoints
- **Лимит:** 60 запросов за минуту
- **Защищает от:** DDoS и перегрузки сервера

### Исключения

GET запросы (кроме `/api/auth/*` и `/api/quiz/get-results`) **не ограничиваются** для лучшей производительности.

## 📊 HTTP Headers

При каждом запросе возвращаются следующие заголовки:

```
X-RateLimit-Limit: 5          # Максимальное количество запросов
X-RateLimit-Remaining: 3      # Оставшееся количество запросов
X-RateLimit-Reset: 2025-01-05T15:30:00.000Z  # Время сброса лимита
```

При превышении лимита возвращается:

**Status:** `429 Too Many Requests`

**Response:**
```json
{
  "error": "Слишком много запросов. Пожалуйста, попробуйте позже.",
  "retryAfter": 900
}
```

**Headers:**
```
Retry-After: 900  # Секунд до следующей попытки
```

## 🔄 Миграция на Redis (для production)

Для production окружения рекомендуется использовать Redis-based rate limiting:

### Вариант 1: Upstash Rate Limit

```bash
npm install @upstash/ratelimit @upstash/redis
```

```typescript
// lib/rate-limit-redis.ts
import { Ratelimit } from '@upstash/ratelimit'
import { Redis } from '@upstash/redis'

const redis = new Redis({
  url: process.env.UPSTASH_REDIS_REST_URL!,
  token: process.env.UPSTASH_REDIS_REST_TOKEN!,
})

export const rateLimiter = new Ratelimit({
  redis,
  limiter: Ratelimit.slidingWindow(5, '15 m'),
})
```

### Вариант 2: Свой Redis

```bash
npm install ioredis
```

```typescript
import Redis from 'ioredis'

const redis = new Redis(process.env.REDIS_URL)

// Использовать Redis для хранения счетчиков
```

## 🧪 Тестирование

### Проверка работы rate limiting

1. **Тест аутентификации:**
```bash
# Отправить 6 запросов подряд
for i in {1..6}; do
  curl -X POST http://localhost:3000/api/auth/telegram/verify-code \
    -H "Content-Type: application/json" \
    -d '{"code":"123456"}'
done
```

2. **Проверка заголовков:**
```bash
curl -I http://localhost:3000/api/newsletter/subscribe
```

Должны вернуться заголовки `X-RateLimit-*`.

## ⚙️ Настройка лимитов

Чтобы изменить лимиты, отредактируйте `lib/rate-limit.ts`:

```typescript
export const RateLimitConfigs = {
  auth: {
    interval: 15 * 60 * 1000, // Изменить интервал
    limit: 5,                  // Изменить лимит
  },
  // ...
}
```

## 📝 Логирование

Rate limiting логирует превышения лимитов только в development режиме:

```typescript
if (process.env.NODE_ENV === 'development') {
  console.log('Rate limit exceeded:', identifier)
}
```

В production логирование отключено для безопасности.

## 🔐 Безопасность

### Идентификация клиентов

Rate limiting использует IP адрес для идентификации клиентов:

1. Проверяет `x-forwarded-for` (для прокси/load balancers)
2. Проверяет `x-real-ip`
3. Использует `request.ip` (NextRequest)
4. Fallback: `'unknown'`

**Важно:** В production за прокси/load balancer нужно передавать реальный IP клиента.

### Обход rate limiting

**Потенциальные проблемы:**
- Использование VPN/proxy для смены IP
- Распределенные атаки с множества IP

**Защита:**
- Использовать более строгие лимиты
- Добавить CAPTCHA для критических операций
- Мониторинг подозрительной активности
- Использовать WAF (Web Application Firewall) на уровне хостинга

## 📚 Дополнительные ресурсы

- [Next.js Middleware](https://nextjs.org/docs/app/building-your-application/routing/middleware)
- [OWASP Rate Limiting](https://owasp.org/www-community/controls/Blocking_Brute_Force_Attacks)
- [Upstash Rate Limit](https://upstash.com/docs/redis/features/ratelimit)

---

**Последнее обновление:** 2025-01-05

