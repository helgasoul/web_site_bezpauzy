# Инструкция по размещению приветственного видео Евы

## 📍 Где размещено видео

Видео добавлено в секцию **"Познакомьтесь с Евой"**, которая находится:
- **Между Hero-секцией и секцией "Как работает Ева"**
- Это первая секция после главного экрана с заголовком

## 📁 Как добавить видео файл

### Вариант 1: В корень папки `public` (рекомендуется)
1. Скопируйте файл `Приветственное видео.mp4` в папку `public/`
2. Переименуйте его в `Приветственное видео.mp4` (или обновите путь в компоненте)

### Вариант 2: В подпапку `public/videos`
1. Создайте папку `public/videos/` (если её нет)
2. Скопируйте файл туда как `eva-intro.mp4`
3. Обновите путь в `components/bot/BotIntroVideo.tsx` на `/videos/eva-intro.mp4`

## 🎨 Что реализовано

### Компонент `BotIntroVideo`
- ✅ Красивая секция с градиентным фоном
- ✅ Preview с кнопкой Play
- ✅ Кастомный видеоплеер с контролами
- ✅ Кнопка включения/выключения звука
- ✅ Кнопка закрытия видео
- ✅ Плавные анимации (Framer Motion)
- ✅ Адаптивный дизайн (mobile/tablet/desktop)

### Расположение на странице
```
/bot страница:
1. BotHero (главный экран)
2. BotIntroVideo (НОВОЕ - приветственное видео) ⭐
3. BotHowItWorks (Как работает Ева)
4. BotExampleConversation (Пример диалога)
5. BotFeatures (Возможности)
6. BotPricing (Тарифы)
7. BotTrust (Безопасность)
8. BotFAQ (FAQ)
9. BotCTA (Призыв к действию)
```

## 🔄 Альтернативные варианты размещения

Если хотите разместить видео в другом месте, вот варианты:

### Вариант A: Вместо фото в Hero
- Заменить правую часть `BotHero` на видео
- Плюсы: сразу видно
- Минусы: может перегрузить Hero

### Вариант B: В секции "Пример диалога"
- Добавить видео рядом с примером чата
- Плюсы: контекстно уместно
- Минусы: менее заметно

### Вариант C: В секции "Как работает Ева"
- Добавить видео как первый шаг
- Плюсы: логично вписывается
- Минусы: может отвлечь от шагов

## ✅ Текущее решение (рекомендуется)

**Отдельная секция сразу после Hero** — это оптимально, потому что:
1. ✅ Видно сразу после главного экрана
2. ✅ Не перегружает Hero
3. ✅ Имеет собственный контекст и заголовок
4. ✅ Пользователь может пропустить, если не хочет смотреть
5. ✅ Красиво оформлено и привлекает внимание

## 🎬 После добавления видео

После того, как вы скопируете видео в `public/`, оно автоматически появится на странице `/bot`.

Если видео не загружается:
1. Проверьте путь к файлу
2. Проверьте формат (должен быть `.mp4`)
3. Проверьте размер файла (рекомендуется < 50MB для быстрой загрузки)
4. Откройте консоль браузера (F12) для проверки ошибок

