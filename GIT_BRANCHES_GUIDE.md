# Руководство по работе с ветками Git

## Что такое ветка (branch)?

**Ветка** — это независимая линия разработки. Представьте, что это копия вашего проекта, где вы можете работать, не затрагивая основную версию.

## Основные ветки

### `main` (или `master`)
- **Назначение:** Стабильная, рабочая версия проекта
- **Когда использовать:** Для финального, проверенного кода
- **Правило:** Код в `main` должен всегда работать!

## Когда создавать дополнительные ветки?

### 1. 🆕 Разработка новой функции

**Пример:** Добавление системы регистрации

```bash
# Создаем ветку для новой функции
git checkout -b feature/user-registration

# Работаем над функцией
# ... делаем изменения ...
git add .
git commit -m "feat: Add user registration form"

# Когда функция готова, объединяем в main
git checkout main
git merge feature/user-registration
git push origin main
```

**Преимущества:**
- ✅ Можно экспериментировать, не ломая основной код
- ✅ Легко откатить изменения (просто удалить ветку)
- ✅ Можно работать над несколькими функциями параллельно

### 2. 🐛 Исправление ошибок

**Пример:** Исправление проблемы с cookie

```bash
# Создаем ветку для исправления
git checkout -b fix/cookie-not-setting

# Исправляем проблему
# ... делаем изменения ...
git add .
git commit -m "fix: Cookie not setting on login"

# Объединяем исправление в main
git checkout main
git merge fix/cookie-not-setting
git push origin main
```

**Преимущества:**
- ✅ Можно быстро исправить критическую ошибку
- ✅ Не затрагиваете другую работу
- ✅ Легко отследить, что было исправлено

### 3. 🧪 Эксперименты

**Пример:** Пробуем новый дизайн

```bash
# Создаем ветку для эксперимента
git checkout -b experiment/new-ui-design

# Пробуем новый дизайн
# ... делаем изменения ...

# Если понравилось - объединяем
git checkout main
git merge experiment/new-ui-design

# Если не понравилось - просто удаляем
git branch -D experiment/new-ui-design
```

### 4. 👥 Работа в команде

**Пример:** Несколько разработчиков работают одновременно

```bash
# Разработчик 1 работает над регистрацией
git checkout -b feature/registration

# Разработчик 2 работает над чатом
git checkout -b feature/chat-improvements

# Разработчик 3 исправляет ошибки
git checkout -b fix/bug-123

# Каждый работает независимо, потом все объединяется в main
```

## Основные команды для работы с ветками

### Создание и переключение

```bash
# Создать новую ветку и переключиться на неё
git checkout -b feature/my-feature

# Или (новый синтаксис)
git switch -c feature/my-feature

# Переключиться на существующую ветку
git checkout main
# или
git switch main

# Посмотреть все ветки
git branch

# Посмотреть все ветки (включая удаленные)
git branch -a
```

### Объединение веток

```bash
# Объединить ветку feature в main
git checkout main
git merge feature/my-feature

# Отправить ветку на GitHub
git push origin feature/my-feature
```

### Удаление веток

```bash
# Удалить локальную ветку (после объединения)
git branch -d feature/my-feature

# Удалить ветку принудительно (если не объединена)
git branch -D feature/my-feature

# Удалить удаленную ветку на GitHub
git push origin --delete feature/my-feature
```

## Рекомендуемые названия веток

### По типу работы:
- `feature/название` - новая функция (например: `feature/user-dashboard`)
- `fix/название` - исправление ошибки (например: `fix/login-redirect`)
- `refactor/название` - рефакторинг кода (например: `refactor/auth-flow`)
- `docs/название` - изменения в документации (например: `docs/update-readme`)
- `test/название` - добавление тестов (например: `test/user-auth`)
- `experiment/название` - эксперименты (например: `experiment/new-design`)

### По номеру задачи:
- `issue-123` - работа над задачей #123
- `bugfix-456` - исправление бага #456

## Workflow (процесс работы)

### Простой workflow для одного разработчика:

```bash
# 1. Начинаем работу над новой функцией
git checkout -b feature/new-feature

# 2. Работаем, делаем коммиты
git add .
git commit -m "feat: Add new feature"

# 3. Когда функция готова, переключаемся на main
git checkout main

# 4. Объединяем изменения
git merge feature/new-feature

# 5. Отправляем на GitHub
git push origin main

# 6. Удаляем ветку (опционально)
git branch -d feature/new-feature
```

### Workflow с тестированием:

```bash
# 1. Создаем ветку для функции
git checkout -b feature/user-registration

# 2. Разрабатываем
# ... код ...

# 3. Тестируем локально
npm run dev
# Проверяем, что все работает

# 4. Коммитим
git add .
git commit -m "feat: Add user registration"

# 5. Отправляем ветку на GitHub для проверки
git push origin feature/user-registration

# 6. Создаем Pull Request на GitHub (для ревью)

# 7. После одобрения объединяем в main
git checkout main
git merge feature/user-registration
git push origin main
```

## Когда НЕ нужны дополнительные ветки?

### Для простых проектов (как ваш сейчас):
- ✅ Если работаете одна
- ✅ Если изменения небольшие
- ✅ Если можно сразу коммитить в `main`

**Можно работать прямо в `main`:**
```bash
git add .
git commit -m "Описание изменений"
git push origin main
```

## Рекомендации для вашего проекта

### Сейчас (начальная стадия):
- ✅ Работайте в `main` — это нормально для начала
- ✅ Делайте понятные коммиты
- ✅ Регулярно отправляйте на GitHub

### В будущем (когда проект вырастет):
- 🔄 Создавайте ветки для больших функций
- 🔄 Используйте ветки для экспериментов
- 🔄 Используйте ветки, если будете работать в команде

## Примеры для вашего проекта

### Сценарий 1: Добавление новой функции "Напоминания"

```bash
# Создаем ветку
git checkout -b feature/reminders

# Разрабатываем функцию
# ... код для напоминаний ...

# Коммитим
git add .
git commit -m "feat: Add reminder system for users"

# Тестируем, если все ОК - объединяем
git checkout main
git merge feature/reminders
git push origin main
```

### Сценарий 2: Исправление критической ошибки

```bash
# Срочно создаем ветку для исправления
git checkout -b hotfix/login-error

# Исправляем
# ... исправление ...

# Коммитим
git add .
git commit -m "fix: Critical login error"

# Сразу объединяем и отправляем
git checkout main
git merge hotfix/login-error
git push origin main
```

## Итог

**Ветки — это инструмент для:**
- 🎯 Организации работы
- 🎯 Изоляции изменений
- 🎯 Безопасного экспериментирования
- 🎯 Командной работы

**Для вашего проекта сейчас:**
- Можно работать в `main` — это нормально
- Ветки понадобятся, когда проект станет больше
- Ветки полезны для экспериментов и больших функций

