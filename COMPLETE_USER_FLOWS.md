# Полное описание всех User Flows

## 🎯 Ключевая логика для общения с Евой

**Важно:** Ева доступна только в личном кабинете, поэтому для общения с Евой всегда нужна регистрация на сайте.

**Упрощенный флоу:**
1. Пользователь хочет общаться с Евой
2. Проверяется сессия на сайте
3. **Если нет сессии:**
   - Показывается экран с кнопками:
     - "Зарегистрироваться" (основная)
     - "Уже зарегистрированы? Войти" (вторичная)
4. **После регистрации/входа:**
   - Проверяется наличие Telegram ID
   - Если нет → экран "Привяжите Telegram"
   - Если есть → проверка подписки → чат или экран оплаты

**Нет функционала "Войти по email"** — этот вариант не реализован и не используется.

---

## 📋 Состояния пользователя

### Состояние 1: Анонимный пользователь
- ❌ Нет регистрации
- ❌ Нет сессии
- ❌ Нет telegram_id
- ❌ Нет логина/пароля

### Состояние 2: Зарегистрирован на сайте (логин/пароль)
- ✅ Есть регистрация на сайте
- ✅ Есть логин/пароль
- ❌ Нет telegram_id (или есть, но не привязан)
- ✅ Есть сессия (после входа)

### Состояние 3: Зарегистрирован в Telegram боте
- ✅ Есть telegram_id
- ✅ Есть запись в menohub_users (создана через бота)
- ❌ Нет логина/пароля на сайте (или есть, но не помнит)
- ❌ Нет сессии на сайте

### Состояние 4: Полностью зарегистрирован
- ✅ Есть логин/пароль на сайте
- ✅ Есть telegram_id (привязан)
- ✅ Есть сессия
- ✅/❌ Есть/нет подписки

---

## 🎯 Точки входа (где пользователь может начать действие)

### Точка входа 1: Header → "Войти в аккаунт"
**Где:** `components/layout/Header.tsx`

### Точка входа 2: Квиз → "Сохранить результаты"
**Где:** `components/quiz/SaveResultsButton.tsx`

### Точка входа 3: Кнопка "Спросить Еву"
**Где:** 
- `components/ui/AskEvaWidget.tsx` (плавающая кнопка)
- `components/quiz/AskEvaQuizButton.tsx` (в квизе)
- `components/bot/BotCTA.tsx` (на странице бота)
- `components/bot/BotHero.tsx` (на главной)

---

## 🔄 Все возможные Flow

### Flow 1: Пользователь просто смотрит сайт

**Состояние:** Анонимный пользователь

**Действия:**
- Читает статьи
- Просматривает контент
- Может подписаться на рассылку (email только, без регистрации)

**Регистрация:** ❌ НЕ нужна

**Результат:** Пользователь может свободно просматривать весь публичный контент

---

### Flow 2: Пользователь хочет сохранить результаты квиза

**Состояние:** Анонимный пользователь

**Действия:**
1. Проходит квиз (MRS или Inflammation)
2. Видит результаты
3. Нажимает "Сохранить результаты"

**Что происходит:**
- Проверяется сессия (cookie `telegram_session`)
- Если сессии нет → показывается модальное окно с опциями:
  - "Зарегистрироваться" → `RegisterModal` (основная кнопка)
  - "Уже зарегистрированы? Войти" → `WebsiteLoginModal` (вторичная кнопка)

**После регистрации/входа:**
- Автоматически сохраняются результаты квиза
- Показывается сообщение "Результаты сохранены!"
- Ссылка "Посмотреть в личном кабинете"

**Регистрация:** ✅ НУЖНА (для сохранения результатов)

**Результат:** Результаты сохранены в личном кабинете

---

### Flow 3: Пользователь хочет пообщаться с Евой (НЕ зарегистрирован)

**Состояние:** Анонимный пользователь

**Действия:**
1. Нажимает "Спросить Еву" (любая точка входа)
2. Открывается `/chat` или виджет

**Что происходит:**
- Проверяется сессия
- Если сессии нет → показывается `ChatAuthGate` с опциями:
  - "Зарегистрироваться" → `RegisterModal` (основная кнопка)
  - "Уже зарегистрированы? Войти" → `WebsiteLoginModal` (вторичная кнопка)

**После регистрации/входа:**
- Проверяется наличие `telegram_id`
- Если `telegram_id` НЕТ → показывается экран "Привяжите Telegram"
  - Кнопка "Привязать Telegram" → `TelegramLinkModal`
  - Генерируется код и deep link `https://t.me/bezpauzy_bot?start=link_КОД`
  - Пользователь нажимает кнопку "Открыть бота" → открывается бот с deep link
  - Бот автоматически обрабатывает код и привязывает telegram_id (реализовано в n8n)
  - После привязки пользователь возвращается на сайт
- Если `telegram_id` ЕСТЬ → проверяется подписка
  - Если подписка активна → открывается чат ✅
  - Если подписки нет → показывается экран "Нужна подписка"

**Регистрация:** ✅ НУЖНА (для общения с Евой)

**Привязка Telegram:** ✅ НУЖНА (для синхронизации с ботом)

**Подписка:** ✅ НУЖНА (для доступа к чату)
- Показывается экран "Нужна подписка"
- Кнопка "Оплатить" → `/payment/subscribe` (будет привязано к ЮКасса через n8n)

**Результат:** Пользователь может общаться с Евой

---

### Flow 4: Пользователь хочет пообщаться с Евой (ЗАРЕГИСТРИРОВАН на сайте)

**Состояние:** Зарегистрирован на сайте (логин/пароль), но нет telegram_id

**Действия:**
1. Нажимает "Спросить Еву"
2. Проверяется сессия → ✅ Есть сессия
3. Проверяется `telegram_id` → ❌ Нет telegram_id

**Что происходит:**
- Показывается экран "Привяжите Telegram"
- Кнопка "Привязать Telegram" → `TelegramLinkModal`
- Генерируется код и deep link `https://t.me/bezpauzy_bot?start=link_КОД`
- Пользователь нажимает кнопку "Открыть бота" → открывается бот с deep link
- Бот автоматически обрабатывает код и привязывает telegram_id (реализовано в n8n)
- После привязки → проверяется подписка
  - Если подписка активна → открывается чат ✅
  - Если подписки нет → показывается экран "Нужна подписка"
    - Кнопка "Оплатить" → `/payment/subscribe` (будет привязано к ЮКасса через n8n)

**Регистрация:** ✅ УЖЕ ЕСТЬ

**Привязка Telegram:** ✅ НУЖНА

**Подписка:** ✅ НУЖНА

**Результат:** Пользователь может общаться с Евой

---

### Flow 5: Пользователь хочет пообщаться с Евой (НЕТ сессии на сайте)

**Состояние:** Нет сессии на сайте (независимо от того, есть ли пользователь в Telegram боте или нет)

**Важно:** Ева доступна только в личном кабинете, поэтому для доступа к Еве нужна регистрация на сайте.

**Действия:**
1. Нажимает "Спросить Еву"
2. Проверяется сессия → ❌ Нет сессии

**Что происходит:**
- Показывается `ChatAuthGate` с двумя опциями:
  - **"Зарегистрироваться"** (основная кнопка) → `RegisterModal`
    - Создание логина/пароля
    - После регистрации предлагается привязать Telegram (опционально)
    - Если пользователь уже есть в Telegram боте, при привязке аккаунты автоматически связываются
  - **"Уже зарегистрированы? Войти"** (вторичная кнопка) → `WebsiteLoginModal`
    - Для пользователей, которые уже зарегистрировались на сайте ранее

**После регистрации/входа:**
1. Если нет Telegram ID → показывается экран "Привяжите Telegram"
2. Если Telegram привязан → проверяется подписка
   - Если подписка активна → открывается чат ✅
   - Если подписки нет → показывается экран "Нужна подписка" с кнопкой "Оплатить"

**Регистрация:** ✅ НУЖНА (на сайте)

**Привязка Telegram:** ✅ НУЖНА (для общения с Евой)

**Подписка:** ✅ НУЖНА (для общения с Евой)

**Результат:** Пользователь может общаться с Евой

---

### Flow 6: Пользователь хочет войти в личный кабинет из Header

**Состояние:** Любое (анонимный или зарегистрированный)

**Действия:**
1. Нажимает "Войти в аккаунт" в Header
2. Проверяется сессия

**Если сессия ЕСТЬ:**
- Редирект на `/account` ✅

**Если сессии НЕТ:**
- Открывается `AuthModal` с опциями:
  - "Зарегистрироваться" → `RegisterModal`
  - "Войти через логин/пароль" → `WebsiteLoginModal`

**После входа:**
- Редирект на `/account`

**Регистрация:** ✅ НУЖНА (если не зарегистрирован)

**Результат:** Пользователь попадает в личный кабинет

---

## 📊 Матрица состояний и действий

| Действие | Анонимный | Сайт (логин/пароль) | Бот (telegram_id) | Полный (оба) |
|----------|-----------|---------------------|-------------------|--------------|
| **Смотреть сайт** | ✅ Без регистрации | ✅ Без регистрации | ✅ Без регистрации | ✅ Без регистрации |
| **Сохранить результаты квиза** | ⚠️ Нужна регистрация | ✅ Можно сохранить | ⚠️ Нужна сессия | ✅ Можно сохранить |
| **Общаться с Евой** | ⚠️ Нужна регистрация на сайте + ТГ + подписка | ⚠️ Нужна привязка ТГ + подписка | ⚠️ Нужна регистрация на сайте + сессия + подписка | ⚠️ Нужна подписка |
| **Войти в кабинет** | ⚠️ Нужна регистрация/вход | ✅ Можно войти | ⚠️ Нужна сессия | ✅ Можно войти |

---

## 🎯 Оптимальные способы входа для разных пользователей

### Для нового пользователя (никогда не регистрировался):
1. **"Зарегистрироваться"** → Создает логин/пароль → Может сохранять результаты
2. Если хочет общаться с Евой → Привязка Telegram → Подписка

### Для пользователя бота (уже есть telegram_id, но нет регистрации на сайте):
1. **"Зарегистрироваться"** → Создает логин/пароль на сайте
2. После регистрации → Привязка Telegram (аккаунты автоматически связываются)
3. Если уже зарегистрирован на сайте ранее → **"Уже зарегистрированы? Войти"** → Ввод логина/пароля

### Для пользователя сайта (есть логин/пароль):
1. **"Войти через логин/пароль"** → Ввод логина/пароля
2. Если хочет общаться с Евой → Привязка Telegram → Подписка

---

---

## ✅ Итоговая логика модалов аутентификации

### ChatAuthGate (для общения с Евой):
**Упрощенная логика:** Ева доступна только в личном кабинете, поэтому нужна регистрация на сайте.

**Опции:**
1. **"Зарегистрироваться"** (основная кнопка)
   - Для всех пользователей без сессии на сайте
   - Создает логин/пароль
   - После регистрации предлагается привязать Telegram (опционально)

2. **"Уже зарегистрированы? Войти"** (вторичная кнопка)
   - Для пользователей, которые уже зарегистрировались на сайте ранее
   - Ввод логина/пароля

### AuthModal (для входа в личный кабинет из Header):
**Упрощенная логика:** Регистрация или вход через логин/пароль.

**Опции входа:**
1. **"Зарегистрироваться"** 
   - Для новых пользователей
   - Создает логин/пароль
   - После регистрации можно привязать Telegram

2. **"Войти через логин/пароль"**
   - Для пользователей, зарегистрированных на сайте
   - Требует логин и пароль

---

## 🔄 Последовательность действий после входа

### После регистрации:
1. Создается запись в `menohub_users` с логином/паролем
2. Устанавливается сессия (cookie)
3. Проверяется наличие `telegram_id`
   - Если нет → показывается предложение привязать Telegram
     - Кнопка "Привязать Telegram" → `TelegramLinkModal`
     - Генерируется deep link `https://t.me/bezpauzy_bot?start=link_КОД`
     - Пользователь нажимает кнопку → открывается бот
     - Бот автоматически обрабатывает код (n8n) и привязывает telegram_id
   - Если есть → можно сразу использовать

### После входа через логин/пароль:
1. Проверяются учетные данные
2. Устанавливается сессия (cookie)
3. Проверяется наличие `telegram_id`
   - Если нет → показывается предложение привязать Telegram
     - Кнопка "Привязать Telegram" → `TelegramLinkModal`
     - Генерируется deep link `https://t.me/bezpauzy_bot?start=link_КОД`
     - Пользователь нажимает кнопку → открывается бот
     - Бот автоматически обрабатывает код (n8n) и привязывает telegram_id
   - Если есть → можно сразу использовать

### После входа через логин/пароль:
1. Проверяются учетные данные
2. Устанавливается сессия (cookie)
3. Проверяется наличие `telegram_id`
   - Если нет → показывается предложение привязать Telegram
   - Если есть → можно использовать

### Процесс привязки Telegram (через deep link):
1. Пользователь нажимает "Привязать Telegram"
2. Открывается `TelegramLinkModal`
3. Генерируется код и deep link `https://t.me/bezpauzy_bot?start=link_КОД`
4. Пользователь нажимает кнопку "Привязать Telegram" → открывается бот с deep link
5. Бот получает параметр `link_КОД` в команде `/start`
6. n8n workflow обрабатывает код:
   - Находит код в таблице `menohub_telegram_auth_codes`
   - Обновляет `telegram_id` в `menohub_users` по `user_id`
   - Помечает код как использованный
7. Пользователь возвращается на сайт
8. Сайт проверяет наличие `telegram_id` → если есть → можно общаться с Евой

---

## 📝 Итоговые рекомендации

### Для AuthModal (Header):
- ✅ "Зарегистрироваться"
- ✅ "Войти через логин/пароль"
- ✅ Добавить подсказки/иконки для ясности

### Для ChatAuthGate:
- ✅ "Зарегистрироваться" (основная кнопка)
- ✅ "Уже зарегистрированы? Войти" (вторичная кнопка)
- ✅ Упрощенная логика: нет регистрации → регистрация, есть регистрация → проверка ТГ

### Для SaveResultsButton:
- ✅ "Зарегистрироваться"
- ✅ "Войти через логин/пароль"
- ❌ Не нужны другие опции (только для сохранения результатов)

### Для экрана "Нужна подписка" (ChatAuthGate):
- ✅ Кнопка "Оплатить" → `/payment/subscribe`
- ⏳ Будет привязано к ЮКасса через n8n workflow
- ⏳ После оплаты → обновление подписки в БД → доступ к чату

---

## 💳 Flow оплаты подписки (через ЮКасса)

### Текущая реализация:
1. Пользователь видит экран "Нужна подписка"
2. Нажимает кнопку "Оплатить"
3. Переходит на `/payment/subscribe` (страница оплаты)

### Планируемая реализация (через n8n):
1. Пользователь нажимает "Оплатить"
2. Вызывается API `/api/payment/subscribe`
3. API создает платеж в ЮКасса
4. Пользователь перенаправляется на страницу оплаты ЮКасса
5. После успешной оплаты:
   - ЮКасса отправляет webhook в n8n
   - n8n обновляет `subscription_status` в `menohub_users`
   - n8n отправляет уведомление пользователю
6. Пользователь возвращается на сайт
7. Сайт проверяет подписку → если активна → открывается чат ✅

