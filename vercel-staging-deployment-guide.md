# Пошаговая инструкция: Развертывание Staging на Vercel

## 📋 Подготовка

### Шаг 1: Проверка проекта

Убедитесь, что:
- ✅ Проект компилируется без ошибок: `npm run build`
- ✅ Все зависимости установлены: `npm install`
- ✅ Код закоммичен в Git (GitHub/GitLab/Bitbucket)

### Шаг 2: Подготовка переменных окружения

Составьте список всех переменных окружения, которые используются в проекте. Обычно это:
- `NEXT_PUBLIC_SUPABASE_URL`
- `NEXT_PUBLIC_SUPABASE_ANON_KEY`
- `SUPABASE_SERVICE_ROLE_KEY` (если используется)
- `N8N_WEBHOOK_URL` (если используется)
- Другие секреты и API ключи

**Важно:** Для staging используйте отдельные значения (staging база данных, staging webhook и т.д.)

---

## 🚀 Развертывание на Vercel

### Шаг 3: Вход в Vercel

1. Перейдите на [vercel.com](https://vercel.com)
2. Войдите через GitHub/GitLab/Bitbucket (рекомендуется) или создайте аккаунт
3. Подтвердите email, если требуется

### Шаг 4: Подключение проекта

1. В дашборде Vercel нажмите **"Add New..."** → **"Project"**
2. Выберите репозиторий с вашим проектом
3. Если репозиторий не виден:
   - Нажмите **"Adjust GitHub App Permissions"**
   - Выберите нужные репозитории
   - Нажмите **"Save"**

### Шаг 5: Настройка проекта

#### 5.1. Основные настройки

- **Project Name:** `bezpauzy-staging` (или другое имя)
- **Framework Preset:** Next.js (определится автоматически)
- **Root Directory:** `./` (если проект в корне репозитория)
- **Build Command:** `npm run build` (по умолчанию)
- **Output Directory:** `.next` (по умолчанию для Next.js)
- **Install Command:** `npm install` (по умолчанию)

#### 5.2. Настройка переменных окружения

Нажмите **"Environment Variables"** и добавьте все необходимые переменные:

```
NEXT_PUBLIC_SUPABASE_URL=https://your-staging-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-staging-anon-key
SUPABASE_SERVICE_ROLE_KEY=your-staging-service-role-key
N8N_WEBHOOK_URL=https://your-n8n-instance.com/webhook/...
```

**Важно:**
- Для каждой переменной выберите окружения: **Production**, **Preview**, **Development**
- Для staging выберите **Preview** или создайте отдельный проект
- Не добавляйте секреты в код! Только через интерфейс Vercel

#### 5.3. Настройка базы данных Supabase для Staging

**Вариант A: Отдельная база данных (рекомендуется)**

1. В Supabase создайте новый проект для staging
2. Скопируйте структуру таблиц из production (SQL миграции)
3. Используйте URL и ключи staging проекта в переменных окружения

**Вариант B: Та же база данных (не рекомендуется для production)**

Используйте те же переменные, что и для production (рискованно!)

### Шаг 6: Первый деплой

1. Нажмите **"Deploy"**
2. Дождитесь завершения сборки (обычно 2-5 минут)
3. Проверьте логи на наличие ошибок

### Шаг 7: Настройка домена

#### 7.1. Добавление staging поддомена

1. В настройках проекта перейдите в **"Domains"**
2. Добавьте домен: `staging.bezpauzy.com`
3. Vercel покажет инструкции по настройке DNS

#### 7.2. Настройка DNS

В панели управления вашего домена (где вы купили `bezpauzy.com`):

1. Найдите раздел **DNS Settings** или **DNS Management**
2. Добавьте CNAME запись:
   - **Type:** CNAME
   - **Name:** `staging` (или `stg`)
   - **Value:** `cname.vercel-dns.com` (или значение, указанное Vercel)
   - **TTL:** 3600 (или Auto)

3. Сохраните изменения

**Примечание:** DNS изменения могут занять до 24 часов, обычно 1-2 часа

#### 7.3. Проверка домена

1. В Vercel дождитесь статуса **"Valid Configuration"** для домена
2. Проверьте доступность: `https://staging.bezpauzy.com`

---

## 🔧 Дополнительные настройки

### Настройка автоматического деплоя

#### Вариант 1: Автоматический деплой из Git (рекомендуется)

1. В настройках проекта → **"Git"**
2. Выберите ветку для staging (например, `staging` или `develop`)
3. При каждом push в эту ветку будет автоматический деплой

#### Вариант 2: Ручной деплой

Используйте кнопку **"Redeploy"** в дашборде Vercel

### Настройка Preview окружений

Vercel автоматически создает preview окружения для каждого Pull Request:
- URL будет вида: `bezpauzy-staging-abc123.vercel.app`
- Используются те же переменные окружения, что и для Preview

### Настройка Production окружения

Для production создайте отдельный проект в Vercel:
- **Project Name:** `bezpauzy-production`
- **Domain:** `bezpauzy.com` (основной домен)
- **Branch:** `main` или `master`
- Используйте production переменные окружения

---

## 🧪 Проверка после деплоя

### Чеклист:

- [ ] Сайт открывается по staging URL
- [ ] Все страницы загружаются корректно
- [ ] Подключение к Supabase работает
- [ ] Авторизация работает
- [ ] Квизы работают и сохраняют результаты
- [ ] PDF генерация работает
- [ ] Чат с Евой работает (если настроен n8n webhook)
- [ ] Все API endpoints отвечают корректно

### Тестирование:

1. **Главная страница:** `https://staging.bezpauzy.com`
2. **Блог:** `https://staging.bezpauzy.com/blog`
3. **Квизы:** `https://staging.bezpauzy.com/quiz/...`
4. **Чат:** `https://staging.bezpauzy.com/chat`
5. **Личный кабинет:** `https://staging.bezpauzy.com/account`

---

## 🔐 Безопасность

### Рекомендации:

1. **Не используйте production ключи в staging**
   - Создайте отдельную базу данных Supabase
   - Используйте отдельные API ключи

2. **Ограничьте доступ к staging**
   - Используйте Basic Auth (опционально)
   - Или ограничьте по IP (через Vercel Edge Config)

3. **Мониторинг ошибок**
   - Настройте интеграцию с Sentry или другим сервисом
   - Проверяйте логи в Vercel Dashboard

---

## 🐛 Решение проблем

### Ошибка сборки

1. Проверьте логи в Vercel Dashboard
2. Убедитесь, что `npm run build` работает локально
3. Проверьте версии Node.js (Vercel использует последнюю LTS)

### Ошибка подключения к Supabase

1. Проверьте переменные окружения
2. Убедитесь, что URL и ключи правильные
3. Проверьте настройки CORS в Supabase

### Домен не работает

1. Проверьте DNS записи (может занять время)
2. Убедитесь, что домен добавлен в Vercel
3. Проверьте SSL сертификат (Vercel выдает автоматически)

### Переменные окружения не работают

1. Убедитесь, что переменные добавлены для правильного окружения
2. После добавления переменных передеплойте проект
3. Проверьте, что переменные начинаются с `NEXT_PUBLIC_` для клиентских

---

## 📚 Полезные ссылки

- [Vercel Documentation](https://vercel.com/docs)
- [Next.js Deployment](https://nextjs.org/docs/deployment)
- [Supabase Documentation](https://supabase.com/docs)
- [Vercel Environment Variables](https://vercel.com/docs/concepts/projects/environment-variables)

---

## 🎯 Следующие шаги

После успешного развертывания staging:

1. ✅ Протестируйте все функции
2. ✅ Настройте автоматический деплой из Git
3. ✅ Настройте мониторинг и логирование
4. ✅ Подготовьте production окружение (отдельный проект в Vercel)
5. ✅ Настройте CI/CD pipeline (опционально)

---

## 💡 Советы

- Используйте **Preview Deployments** для тестирования PR
- Настройте **Vercel Analytics** для мониторинга производительности
- Используйте **Vercel Speed Insights** для оптимизации
- Настройте **Webhooks** для уведомлений о деплоях

---

**Готово!** Ваш staging сайт должен быть доступен по адресу `https://staging.bezpauzy.com`

Если возникнут вопросы или проблемы, проверьте логи в Vercel Dashboard или обратитесь к документации.

