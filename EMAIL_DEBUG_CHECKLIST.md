# Чеклист для проверки отправки писем о покупке

## Что проверить, если письма не приходят:

### 1. Настройка переменных окружения

Убедитесь, что в `.env.local` настроены:
```env
RESEND_API_KEY=re_ваш_ключ_здесь
RESEND_FROM_EMAIL=Без |Паузы <noreply@bezpauzy.com>
```

**Как получить ключ Resend:**
1. Зарегистрируйтесь на [resend.com](https://resend.com)
2. Перейдите в Dashboard → API Keys
3. Создайте новый API ключ
4. Скопируйте ключ (начинается с `re_`)

**Для тестирования можно использовать тестовый email:**
```env
RESEND_FROM_EMAIL=Без |Паузы <onboarding@resend.dev>
```

### 2. Проверка логов

После успешной оплаты проверьте логи в консоли:

**Должны появиться сообщения:**
- `📧 [Email] Отправка письма с подтверждением покупки:`
- `✅ [Email] Письмо успешно отправлено на: email@example.com`
- `✅ [Email] ID письма в Resend: ...`

**Если видите ошибки:**
- `❌ [Email] RESEND_API_KEY is not configured` - не настроен ключ
- `❌ [Email] Ошибка отправки письма:` - проверьте детали ошибки

### 3. Проверка webhook YooKassa

Проверьте логи webhook после оплаты:

**В логах должно быть:**
- `✅ [YooKassa webhook] Заказ книги/ресурса успешно обновлен на "paid"`
- `📧 [YooKassa webhook] Подготовка к отправке письма`
- `✅ [YooKassa webhook] Письмо с подтверждением покупки отправлено успешно`

### 4. Проверка в Resend Dashboard

1. Войдите в [Resend Dashboard](https://resend.com/emails)
2. Перейдите в раздел "Emails"
3. Проверьте, появляются ли письма в логе отправки
4. Если письма есть, но не приходят - проверьте статус (delivered/failed/bounced)

### 5. Проверка email адреса получателя

- Проверьте правильность email адреса
- Проверьте папку "Спам"
- Проверьте, что домен отправителя не заблокирован

### 6. Временная задержка

Webhook от YooKassa может прийти с задержкой (обычно несколько секунд). 
Если письмо не пришло сразу, подождите 1-2 минуты.

### 7. Development режим

В development режиме (если `RESEND_API_KEY` не настроен) письма не отправляются, только логируются:
```
📧 [DEV] Purchase confirmation email would be sent to: email@example.com
```

Это нормально для разработки.

### 8. Частые проблемы

**Проблема:** `RESEND_API_KEY is not configured`
**Решение:** Добавьте ключ в `.env.local` и перезапустите сервер

**Проблема:** `Invalid API key`
**Решение:** Проверьте правильность ключа, скопируйте его полностью

**Проблема:** `Domain not verified`
**Решение:** Используйте тестовый email `onboarding@resend.dev` или верифицируйте домен в Resend

**Проблема:** Письма отправляются, но не доходят
**Решение:** Проверьте папку "Спам", убедитесь, что email адрес правильный

### 9. Тестирование вручную

Если нужно протестировать отправку письма, можно вызвать функцию напрямую через API:

```bash
curl -X POST http://localhost:3000/api/test-send-email \
  -H "Content-Type: application/json" \
  -d '{
    "email": "your@email.com",
    "name": "Test User",
    "items": [{
      "type": "book",
      "title": "Тестовая книга",
      "downloadToken": "test-token",
      "downloadUrl": "https://bezpauzy.com/api/book/download/test-token",
      "expiresAt": "2025-12-31T23:59:59Z"
    }]
  }'
```

(Нужно создать тестовый endpoint)

### 10. Контрольный список

- [ ] `RESEND_API_KEY` установлен в `.env.local`
- [ ] `RESEND_FROM_EMAIL` установлен в `.env.local`
- [ ] Сервер перезапущен после изменения `.env.local`
- [ ] В логах есть сообщение о попытке отправки письма
- [ ] В Resend Dashboard видны отправленные письма
- [ ] Проверена папка "Спам" получателя
- [ ] Email адрес получателя правильный
