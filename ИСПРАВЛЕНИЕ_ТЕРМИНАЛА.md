# Как открыть терминал в Cursor

## Способ 1: Горячие клавиши

Попробуйте эти комбинации (по очереди):

1. **`` Ctrl + ` ``** (обратная кавычка, обычно над Tab)
2. **`Ctrl + Shift + ``** (обратная кавычка)
3. **`Cmd + J`** (переключает панель внизу)
4. **`Ctrl + Shift + P`** → введите "Terminal: Create New Terminal"

## Способ 2: Через меню

1. В верхнем меню: **`View`** → **`Terminal`**
2. Или: **`Terminal`** → **`New Terminal`**

## Способ 3: Через панель команд

1. Нажмите **`Cmd + Shift + P`** (или `Ctrl + Shift + P`)
2. Введите: **`Terminal: Create New Terminal`**
3. Нажмите Enter

## Способ 4: Через Tasks (если терминал не открывается)

Я создал файл `.vscode/tasks.json` с задачей для запуска сервера:

1. Нажмите **`Cmd + Shift + P`**
2. Введите: **`Tasks: Run Task`**
3. Выберите: **`Start Dev Server`**

Это запустит сервер даже если терминал не открывается!

## Способ 5: Проверка настроек

Если ничего не работает, проверьте настройки:

1. **`Cmd + ,`** (открыть настройки)
2. В поиске введите: **`terminal`**
3. Проверьте:
   - `Terminal > Integrated: Default Profile` должен быть `zsh` или `bash`
   - `Terminal > Integrated: Enabled` должен быть включен

## Способ 6: Перезапуск Cursor

1. Закройте Cursor полностью (`Cmd + Q`)
2. Откройте снова
3. Попробуйте открыть терминал снова

## Способ 7: Проверка прав доступа

Если терминал не открывается, возможно проблема с правами:

1. Откройте **Системные настройки** Mac
2. Перейдите в **Безопасность и конфиденциальность**
3. Проверьте, что Cursor имеет разрешения на доступ к терминалу

## Альтернатива: Использовать Tasks для запуска

Я создал конфигурацию задач. Вы можете запустить сервер через Tasks:

1. **`Cmd + Shift + P`**
2. **`Tasks: Run Task`**
3. **`Start Dev Server`**

Сервер запустится в фоновом режиме, и вы увидите вывод в панели "Terminal" (даже если она не открывалась раньше).

## Если ничего не помогает

Попробуйте:
1. Обновить Cursor до последней версии
2. Переустановить Cursor
3. Использовать обычный VS Code (он точно поддерживает терминал)

## Проверка работы

После запуска сервера (любым способом):
- Откройте браузер
- Перейдите на `http://localhost:3000`
- Если страница загружается — всё работает! 🎉
