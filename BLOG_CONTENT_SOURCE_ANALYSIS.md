# Анализ источника контента статей блога

## 🔍 Вопрос: Откуда берется текст на странице статьи?

### Текущая ситуация:

1. **В `app/blog/[slug]/page.tsx`:**
   - Функция `getArticle(slug)` проверяет объект `articles`
   - Если статья найдена → возвращает объект статьи
   - Если статья НЕ найдена → возвращает `null` → вызывается `notFound()` → показывается 404

2. **В объекте `articles` (строка 14-169):**
   - Найдено только **ОДНА статья**: `'prilivy-prichiny-i-resheniya'`
   - Размер блока: ~7800 символов
   - Полный контент только у этой статьи

3. **В `BlogListing.tsx`:**
   - Перечислено 7 статей с заголовками и excerpt
   - Но это только для отображения списка статей

### ⚠️ ПРОБЛЕМА:

Если пользователь кликает на статью из списка (например, "Маммография после 40"), то:
1. Открывается URL: `/blog/mammografiya-posle-40`
2. `getArticle('mammografiya-posle-40')` ищет в объекте `articles`
3. Не находит → возвращает `null`
4. Вызывается `notFound()` → показывается **404 страница**

### ❓ Возможные объяснения:

1. **Пользователь видит 404 страницы** для статей без контента
2. **Или статьи были написаны ранее**, но файл был изменен/перезаписан, и осталась только одна статья
3. **Или есть отдельные страницы**, которые я не нашел (но `find` показал только `app/blog/[slug]/page.tsx`)
4. **Или контент загружается из Supabase**, но в коде этого нет (есть только комментарий "will be replaced with Supabase query")

### ✅ ОТВЕТ НА ВОПРОС:

**Откуда берется текст на странице статьи?**

1. **Для статьи "Приливы"** (`/blog/prilivy-prichiny-i-resheniya`):
   - Текст берется из объекта `articles` в `app/blog/[slug]/page.tsx` (строки 15-168)
   - Статья с полным контентом ✅

2. **Для остальных статей** (например, `/blog/mammografiya-posle-40`):
   - `getArticle()` не находит статью в объекте `articles`
   - Возвращает `null`
   - Вызывается `notFound()` 
   - **Показывается 404 страница** - текста НЕТ ❌

### ✅ РЕШЕНИЕ:

**Сейчас только ОДНА статья работает** - при клике на остальные показывается 404.

**Чтобы статьи работали, нужно:**

1. **Вариант 1:** Добавить полный контент для всех статей в объект `articles` в `app/blog/[slug]/page.tsx`
2. **Вариант 2:** Реализовать загрузку из Supabase (есть комментарий "will be replaced with Supabase query")
3. **Вариант 3:** Создать отдельные страницы для каждой статьи (например, `app/blog/mammografiya-posle-40/page.tsx`)

### 📊 Текущий статус:

- ✅ **1 статья работает:** `/blog/prilivy-prichiny-i-resheniya` - полный контент есть
- ❌ **6 статей показывают 404:** все остальные из списка BlogListing

---

**Вывод:** 
- Если пользователь видит текст на странице статьи → он открыл статью "Приливы"
- Если пользователь видит 404 → он открыл любую другую статью из списка
- Текст для других статей **НЕ существует** - его нужно добавить

**Дата анализа:** 3 января 2024

