# 📄 Конвертер PDF в Markdown

Скрипт для автоматической конвертации PDF файлов в формат Markdown.

## 🚀 Быстрый старт

### 1. Установите зависимость

```bash
npm install pdf-parse
```

### 2. Используйте скрипт

#### Конвертировать один файл:

```bash
npm run pdf-to-md "litrature/Загруженные в Без|паузы/HRT – Practical prescribing.pdf" "litrature/zgt_text"
```

#### Конвертировать все PDF в папке:

```bash
npm run pdf-to-md "litrature/Загруженные в Без|паузы/" "litrature/zgt_text" --all
```

## 📋 Примеры использования

### Пример 1: Конвертировать один файл о ЗГТ

```bash
npm run pdf-to-md \
  "litrature/Загруженные в Без|паузы/HRT – Practical prescribing.pdf" \
  "litrature/zgt_text"
```

### Пример 2: Конвертировать все PDF о ЗГТ

```bash
npm run pdf-to-md \
  "litrature/Загруженные в Без|паузы/" \
  "litrature/zgt_text" \
  --all
```

### Пример 3: Прямой вызов скрипта

```bash
node scripts/pdf-to-markdown.js \
  "litrature/Загруженные в Без|паузы/24.0_pp_177_186_The_Use_of_Estrogens_and_Progestogens_in_Menopausal_Hormone_Therapy.pdf" \
  "litrature/zgt_text"
```

## ✨ Что делает скрипт

1. ✅ Читает PDF файл
2. ✅ Извлекает текст и метаданные (автор, дата создания)
3. ✅ Автоматически форматирует в Markdown:
   - Определяет заголовки и подзаголовки
   - Форматирует списки
   - Разбивает длинные абзацы
4. ✅ Сохраняет результат в указанную папку

## 📁 Формат выходного файла

- **Имя файла**: `имя-pdf-файла.md`
- **Структура**:
  ```markdown
  # Название документа
  
  ## Источник
  [Название из метаданных]
  
  **Автор:** [если есть]
  **Дата создания:** [если есть]
  **Дата конвертации:** [текущая дата]
  
  ---
  
  [Отформатированный текст]
  ```

## ⚠️ Примечания

- Скрипт автоматически определяет структуру документа, но для лучших результатов может потребоваться ручная правка
- Изображения из PDF не извлекаются (только текст)
- Сложные таблицы могут потребовать ручного форматирования

## 🔧 Устранение проблем

### Ошибка: "библиотека pdf-parse не установлена"

```bash
npm install pdf-parse
```

### Ошибка: "Файл не найден"

Убедитесь, что:
- Путь к файлу указан правильно
- Файл существует
- Используйте кавычки для путей с пробелами

### Проблемы с кодировкой

Если в выходном файле неправильная кодировка, скрипт автоматически использует UTF-8. Если проблемы остаются, проверьте исходный PDF.

## 📚 Дополнительная информация

Подробная документация: `scripts/README_PDF_CONVERTER.md`

