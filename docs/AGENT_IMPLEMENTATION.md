# Реализация LangChain агента без n8n

## ✅ Что реализовано

### 1. LangChain Agent (`lib/ai/agent.ts`)
- Создание агента с инструментами
- Использование OpenAI (как в n8n)
- Интеграция с системой обработки сообщений

### 2. Инструменты агента (`lib/ai/agent-tools.ts`)
- **search_doctors**: Поиск врачей в Supabase (единственный источник данных)

### 3. Интеграция в message-processor
- Автоматическое определение, когда использовать агента
- Переключение между обычным ответом и агентом

## 📦 Установка зависимостей

```bash
npm install @langchain/openai @langchain/core @langchain/community openai
```

Если команда не выполнилась, выполните вручную в терминале.

## 🔧 Настройка

### Переменные окружения

Добавьте в `.env.local`:

```env
# OpenAI для агента
OPENAI_API_KEY=sk-...

# Опционально: модель (по умолчанию gpt-4o-mini)
OPENAI_MODEL=gpt-4o-mini
```

## 🎯 Как работает

### Автоматическое определение использования агента

Агент используется автоматически, если сообщение содержит ключевые слова:
- "найди врача"
- "найти врача"
- "специалист"
- "клиника"
- "гинеколог", "маммолог", "сомнолог" и т.д.

### Flow обработки:

1. **Проверка безопасности** (Lakera)
2. **Определение типа запроса**:
   - Если нужен поиск врачей → используется агент
   - Если обычный вопрос → используется Claude с RAG
3. **Обработка через агента**:
   - Агент решает, какие инструменты использовать
   - Вызывает search_doctors или research
   - Формирует ответ на основе результатов
4. **Возврат ответа**

## 🔍 Инструменты

### search_doctors

Поиск врачей в таблице `menohub_doctors`:
- Фильтр по специальности
- Фильтр по городу
- Возвращает список с контактами и рейтингами
- **Единственный источник данных** - база данных Supabase
- Если врач не найден, честно сообщает об этом пользователю

## 📝 Примеры использования

### Запрос, который использует агента:

```
Пользователь: "Найди мне гинеколога в Москве"
→ Агент определяет, что нужен search_doctors
→ Вызывает инструмент с query="гинеколог", city="Москва"
→ Получает список врачей
→ Формирует ответ
```

### Обычный запрос (без агента):

```
Пользователь: "Что такое менопауза?"
→ Используется обычная генерация через Claude
→ RAG поиск в базе знаний
→ Генерация ответа
```

## 🚀 Следующие шаги

1. **Установить зависимости** (если еще не установлены)
2. **Добавить OPENAI_API_KEY** в `.env.local`
3. **Протестировать** запросы с поиском врачей
4. **Интегрировать web search** для research инструмента (опционально)

## ⚠️ Важные замечания

1. **Модель OpenAI**: По умолчанию используется `gpt-4o-mini`. Можно изменить через `OPENAI_MODEL`
2. **Источник данных**: Все данные берутся только из базы данных Supabase. Интернет-поиск не используется
3. **Стоимость**: Агент использует больше токенов, чем обычная генерация (из-за function calling)
4. **Ограничения**: `maxIterations: 5` - максимум 5 итераций агента
5. **Если врач не найден**: Агент честно сообщает об этом и рекомендует обратиться к лечащему врачу

## 🔄 Миграция с n8n

Текущая реализация:
- ✅ Агент с инструментами
- ✅ search_doctors (поиск в Supabase)
- ❌ research (удален - данные только из БД)

Отличия от n8n:
- Не используем интернет-поиск
- Все данные только из базы данных
- Более простая и надежная система
