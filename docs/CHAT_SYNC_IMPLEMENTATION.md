# Реализация синхронизации чата между Telegram и сайтом

## ✅ Что реализовано

### 1. Единый обработчик сообщений

**Файл:** `lib/ai/message-processor.ts`

- Обрабатывает сообщения одинаково для Telegram и сайта
- Проверяет безопасность через Lakera AI
- Получает контекст из RAG системы
- Генерирует ответ через Claude API
- Использует историю разговора для контекста

### 2. Обработка сообщений из Telegram

**Файл:** `lib/telegram/message-handler.ts`

- Получает сообщения из Telegram webhook
- Сохраняет в `menohub_queries` с `source='telegram'`
- Обрабатывает через единый процессор
- Отправляет ответ обратно в Telegram

### 3. Обработка сообщений с сайта

**Файл:** `app/api/chat/send-message/route.ts`

- Обновлен для использования единого процессора
- Сохраняет с `source='website'`
- Возвращает ответ сразу на сайт

### 4. История сообщений

**Файл:** `app/api/chat/history/route.ts`

- Обновлен для включения поля `source`
- Показывает все сообщения независимо от источника
- История синхронизирована между каналами

### 5. AI интеграции

- **Claude API** (`lib/ai/claude.ts`) - генерация ответов
- **Lakera AI** (`lib/ai/lakera.ts`) - защита от промпт-инжекции
- **RAG система** (`lib/ai/rag.ts`) - поиск в базе знаний

## 🔄 Как это работает

### Сценарий 1: Пользователь пишет в Telegram

```
1. Пользователь → Telegram → /api/telegram/webhook
2. message-handler.ts → сохраняет в БД (source='telegram')
3. processMessage() → проверка безопасности → RAG → Claude
4. Ответ сохраняется в БД
5. Ответ отправляется в Telegram
6. Если пользователь открыт на сайте → polling обновит историю
```

### Сценарий 2: Пользователь пишет на сайте

```
1. Пользователь → Сайт → /api/chat/send-message
2. API → сохраняет в БД (source='website')
3. processMessage() → проверка безопасности → RAG → Claude
4. Ответ сохраняется в БД
5. Ответ возвращается на сайт
6. Если пользователь открыт в Telegram → можно отправить уведомление (опционально)
```

### Сценарий 3: Пользователь видит историю

```
1. Сайт → /api/chat/history
2. API → получает все сообщения по user_id
3. Включает сообщения с source='telegram' и source='website'
4. Показывает единую историю
```

## 📊 Структура данных

### Таблица menohub_queries

```sql
menohub_queries (
  id UUID PRIMARY KEY,
  user_id BIGINT REFERENCES menohub_users(id),
  query_text TEXT,
  response_text TEXT,
  query_status TEXT, -- 'pending', 'processing', 'completed', 'failed'
  source TEXT, -- 'telegram' или 'website'
  telegram_message_id BIGINT, -- ID сообщения в Telegram (если из Telegram)
  created_at TIMESTAMPTZ,
  updated_at TIMESTAMPTZ
)
```

**Рекомендуется добавить колонку `source` в БД:**
```sql
ALTER TABLE menohub_queries 
ADD COLUMN IF NOT EXISTS source TEXT DEFAULT 'website';

ALTER TABLE menohub_queries 
ADD COLUMN IF NOT EXISTS telegram_message_id BIGINT;
```

## 🚀 Следующие шаги

### 1. Установить зависимости

```bash
npm install @anthropic-ai/sdk
```

### 2. Добавить переменные окружения

```env
ANTHROPIC_API_KEY=sk-ant-...
LAKERA_API_KEY=03cfefe9b3d7bf4768971d846d863d779272e71b74eeb2ed2f5acd290cabd470
```

### 3. Обновить базу данных

Добавить колонки `source` и `telegram_message_id` в таблицу `menohub_queries`.

### 4. Протестировать

1. Отправить сообщение в Telegram → проверить на сайте
2. Отправить сообщение на сайте → проверить историю
3. Проверить, что история синхронизирована

## 🎨 UI улучшения (опционально)

### Индикатор источника сообщения

В компоненте чата можно показывать, откуда пришло сообщение:

```tsx
{message.source === 'telegram' && (
  <span className="text-xs text-gray-500">📱 Telegram</span>
)}
{message.source === 'website' && (
  <span className="text-xs text-gray-500">🌐 Сайт</span>
)}
```

### Уведомление о новых сообщениях

Если пользователь получил сообщение в Telegram, на сайте можно показать:

```tsx
{hasNewTelegramMessages && (
  <div className="notification">
    🔔 У вас есть новое сообщение от Евы в Telegram
  </div>
)}
```

## ✅ Преимущества реализации

1. **Единая история** - все сообщения в одном месте
2. **Единая логика** - один код обработки
3. **Гибкость** - можно начать в Telegram, продолжить на сайте
4. **Удобство** - пользователь видит весь контекст
5. **Масштабируемость** - легко добавить новые каналы

## 🔍 Отладка

### Проверка синхронизации

```sql
-- Посмотреть все сообщения пользователя
SELECT 
  id,
  query_text,
  response_text,
  source,
  created_at
FROM menohub_queries
WHERE user_id = <user_id>
ORDER BY created_at DESC;
```

### Логи

Проверьте логи в консоли:
- `✅ [Telegram]` - успешная обработка из Telegram
- `✅ [Website]` - успешная обработка с сайта
- `❌ [Error]` - ошибки обработки

## 📝 Примечания

- Все сообщения сохраняются в одной таблице
- История автоматически синхронизируется
- Пользователь может переключаться между каналами
- Контекст разговора сохраняется
