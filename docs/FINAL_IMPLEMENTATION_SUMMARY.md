# Итоговая реализация: Два агента без n8n

## ✅ Что реализовано

### 1. Основной агент (AI Agent) - для ответов на вопросы

**Файлы:**
- `lib/ai/main-agent.ts` - создание и запуск
- `lib/ai/main-agent-tools.ts` - инструменты

**Инструменты:**
- ✅ `Vector_store` - векторный поиск в `menohub_documents`
- ✅ `Get_user_age` - получение возраста для персонализации
- ✅ `Update_city` - обновление города пользователя

**Используется для:**
- Обычные медицинские вопросы
- Вопросы о симптомах, лечении
- Общие вопросы о здоровье

### 2. Агент для поиска врачей (AI Agent1) - для поиска врачей

**Файлы:**
- `lib/ai/agent.ts` - создание и запуск
- `lib/ai/agent-tools.ts` - инструменты

**Инструменты:**
- ✅ `search_doctors` - поиск в таблице `menohub_doctors`

**Используется для:**
- Запросы "найди врача", "нужен специалист"
- Поиск клиник
- Рекомендации врачей

### 3. Единый обработчик

**Файл:** `lib/ai/message-processor.ts`

- Автоматически определяет, какой агент использовать
- Обрабатывает сообщения из Telegram и с сайта
- Синхронизирует историю между каналами

## 📋 Переменные окружения

```env
# OpenAI (для обоих агентов)
OPENAI_API_KEY=sk-...
OPENAI_MODEL=gpt-4o-mini

# Supabase
NEXT_PUBLIC_SUPABASE_URL=...
NEXT_PUBLIC_SUPABASE_ANON_KEY=...
SUPABASE_SERVICE_ROLE_KEY=... (для векторного поиска)

# Lakera (защита)
LAKERA_API_KEY=...

# Telegram
TELEGRAM_BOT_TOKEN=...
```

## 📦 Зависимости

```bash
npm install @langchain/openai @langchain/core @langchain/community openai
```

## 🔄 Логика работы

```
Сообщение пользователя
    ↓
Проверка безопасности (Lakera)
    ↓
Определение типа запроса
    ↓
┌─────────────────┬─────────────────┐
│ Поиск врачей?   │ Обычный вопрос? │
└─────────────────┴─────────────────┘
    ↓                    ↓
Агент врачей      Основной агент
(search_doctors)  (Vector_store + инструменты)
    ↓                    ↓
Ответ пользователю
```

## 📊 Таблицы в Supabase

### Для основного агента:
- `menohub_documents` - векторная база знаний (с embeddings)
- `menohub_users` - пользователи (age, age_group, city, telegram_id)

### Для агента врачей:
- `menohub_doctors` - врачи (specialty, city, name, booking_link, rating)

## ⚠️ Важные требования

1. **Векторная база знаний** (`menohub_documents`):
   - Должна быть настроена с pgvector
   - Должна иметь функцию `match_menohub_documents`
   - Должна содержать embeddings

2. **Service Role Key**:
   - Нужен для векторного поиска
   - Используется только на сервере

3. **Зависимости**:
   - LangChain пакеты для агентов
   - OpenAI для embeddings и агентов

## ✅ Преимущества реализации

1. **Два специализированных агента** - как в n8n
2. **Все данные из БД** - надежно и контролируемо
3. **Единая обработка** - для Telegram и сайта
4. **Синхронизация** - история в одном месте
5. **Без n8n** - все в одном проекте

## 📝 Отличия от n8n

### Убрано:
- ❌ Research (Perplexity) - интернет-поиск
- ❌ n8n workflow - все в коде

### Сохранено:
- ✅ Два агента (основной + для врачей)
- ✅ Векторный поиск в базе знаний
- ✅ Инструменты для персонализации
- ✅ Структурированные ответы

## 🚀 Готово к использованию

После установки зависимостей и настройки переменных окружения оба агента будут работать без n8n!
