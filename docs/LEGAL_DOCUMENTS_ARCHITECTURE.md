# Архитектура юридических документов

## 📋 Обзор

Юридические документы на сайте: политика конфиденциальности, пользовательское соглашение, оферта и т.д.

---

## ✅ Рекомендация: Статические страницы (БЕЗ таблицы в Supabase)

### Почему статические страницы лучше:

1. **Редко меняются**
   - Юридические документы обновляются нечасто
   - Обычно после юридической проверки
   - Не нужна админка для ежедневного редактирования

2. **Версионирование через Git**
   - История изменений в Git
   - Легко откатить изменения
   - Прозрачность для команды
   - Можно видеть, кто и когда изменил

3. **SEO-friendly**
   - Статические страницы индексируются лучше
   - Быстрая загрузка
   - Нет запросов к БД

4. **Простота**
   - Не нужна админка
   - Не нужна таблица в БД
   - Меньше кода для поддержки

5. **Безопасность**
   - Нет риска SQL-инъекций
   - Нет риска несанкционированного редактирования
   - Контроль через Git и деплой

---

## 📁 Структура файлов

### Рекомендуемая структура:

```
app/
  legal/
    privacy/
      page.tsx          # Политика конфиденциальности
    terms/
      page.tsx          # Пользовательское соглашение
    offer/
      page.tsx          # Публичная оферта (для платных товаров)
    cookies/
      page.tsx          # Политика использования cookies
```

### Альтернативная структура (если документов много):

```
app/
  legal/
    [slug]/
      page.tsx          # Динамический роут для всех документов
```

---

## 🎨 Пример реализации

### Вариант 1: Отдельные страницы (рекомендуется)

**`app/legal/privacy/page.tsx`:**
```typescript
import { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'Политика конфиденциальности | Без |Паузы',
  description: 'Политика конфиденциальности и обработки персональных данных',
  robots: 'index, follow',
}

export default function PrivacyPage() {
  return (
    <main className="min-h-screen bg-soft-white py-16">
      <div className="container mx-auto px-4 max-w-4xl">
        <h1 className="text-h1 font-bold text-deep-navy mb-8">
          Политика конфиденциальности
        </h1>
        
        <div className="prose prose-lg max-w-none">
          {/* Контент политики */}
          <section>
            <h2>1. Общие положения</h2>
            <p>...</p>
          </section>
          
          <section>
            <h2>2. Сбор персональных данных</h2>
            <p>...</p>
          </section>
          
          {/* ... остальные разделы ... */}
        </div>
        
        <div className="mt-8 text-sm text-deep-navy/60">
          <p>Последнее обновление: 25 декабря 2024 г.</p>
          <p>Версия: 1.0</p>
        </div>
      </div>
    </main>
  )
}
```

### Вариант 2: Контент в отдельных файлах (для длинных документов)

**`content/legal/privacy.md`:**
```markdown
# Политика конфиденциальности

## 1. Общие положения
...

## 2. Сбор персональных данных
...
```

**`app/legal/privacy/page.tsx`:**
```typescript
import { readFile } from 'fs/promises'
import { join } from 'path'
import { remark } from 'remark'
import html from 'remark-html'

export default async function PrivacyPage() {
  const filePath = join(process.cwd(), 'content', 'legal', 'privacy.md')
  const fileContents = await readFile(filePath, 'utf8')
  const processedContent = await remark().use(html).process(fileContents)
  const contentHtml = processedContent.toString()
  
  return (
    <main className="min-h-screen bg-soft-white py-16">
      <div className="container mx-auto px-4 max-w-4xl">
        <div 
          className="prose prose-lg max-w-none"
          dangerouslySetInnerHTML={{ __html: contentHtml }}
        />
      </div>
    </main>
  )
}
```

---

## 📋 Список необходимых документов

### Обязательные документы:

1. **Политика конфиденциальности** (`/legal/privacy`)
   - Обработка персональных данных (ФЗ-152)
   - GDPR compliance (если есть пользователи из ЕС)
   - Какие данные собираем
   - Как используем
   - Права пользователей

2. **Пользовательское соглашение** (`/legal/terms`)
   - Условия использования сайта
   - Правила поведения
   - Ответственность сторон
   - Интеллектуальная собственность

3. **Публичная оферта** (`/legal/offer`)
   - Для платных товаров (гайды, книги)
   - Условия покупки
   - Возврат средств
   - Срок действия ссылок

4. **Политика cookies** (`/legal/cookies`)
   - Какие cookies используем
   - Для чего
   - Как отключить

### Опциональные документы:

5. **Правила сообщества** (`/legal/community`)
   - Если есть форум/сообщество

6. **Политика возврата** (`/legal/refund`)
   - Если нужна отдельная страница

---

## 🔄 Когда использовать таблицу в Supabase?

### Используйте таблицу, если:

1. **Нужна админка для редактирования**
   - Не-технические сотрудники редактируют документы
   - Частые изменения без деплоя
   - Нет доступа к Git

2. **Нужна история изменений в БД**
   - Отслеживание всех версий
   - Аудит изменений
   - Восстановление старых версий

3. **Многоязычность**
   - Документы на разных языках
   - Динамический выбор языка

4. **Персонализация**
   - Разные версии для разных регионов
   - A/B тестирование текстов

### Структура таблицы (если нужна):

```sql
CREATE TABLE menohub_legal_documents (
    id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
    slug TEXT UNIQUE NOT NULL, -- 'privacy', 'terms', 'offer'
    title TEXT NOT NULL,
    content TEXT NOT NULL, -- HTML или Markdown
    version TEXT NOT NULL, -- '1.0', '1.1'
    language TEXT DEFAULT 'ru', -- 'ru', 'en'
    published BOOLEAN DEFAULT TRUE,
    published_at TIMESTAMPTZ DEFAULT NOW(),
    created_at TIMESTAMPTZ DEFAULT NOW(),
    updated_at TIMESTAMPTZ DEFAULT NOW(),
    created_by BIGINT REFERENCES menohub_users(id)
);

-- История версий
CREATE TABLE menohub_legal_documents_history (
    id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
    document_id UUID REFERENCES menohub_legal_documents(id),
    content TEXT NOT NULL,
    version TEXT NOT NULL,
    changed_by BIGINT REFERENCES menohub_users(id),
    changed_at TIMESTAMPTZ DEFAULT NOW()
);
```

---

## ✅ Рекомендация для вашего проекта

**Используйте статические страницы** по следующим причинам:

1. ✅ Документы редко меняются
2. ✅ Нужна юридическая проверка перед публикацией
3. ✅ Версионирование через Git лучше для юридических документов
4. ✅ Нет необходимости в админке
5. ✅ Проще для SEO
6. ✅ Меньше кода для поддержки

**Исключение:** Если в будущем понадобится админка для частого редактирования, можно мигрировать на таблицу.

---

## 📝 План реализации

### Этап 1: Создать структуру папок
```
app/
  legal/
    privacy/
      page.tsx
    terms/
      page.tsx
    offer/
      page.tsx
    cookies/
      page.tsx
```

### Этап 2: Создать страницы
- Использовать шаблон из `docs/OFFER_TEMPLATE.md` для оферты
- Создать политику конфиденциальности
- Создать пользовательское соглашение
- Создать политику cookies

### Этап 3: Добавить ссылки в Footer
- Обновить `components/layout/Footer.tsx`
- Добавить ссылки на все юридические документы

### Этап 4: SEO оптимизация
- Добавить мета-теги
- Добавить structured data
- Добавить в sitemap

---

## 🔗 Ссылки в Footer

Обновить `components/layout/Footer.tsx`:

```tsx
<div className="flex flex-col gap-2">
  <Link href="/legal/privacy" className="hover:text-ocean-wave-start">
    Политика конфиденциальности
  </Link>
  <Link href="/legal/terms" className="hover:text-ocean-wave-start">
    Пользовательское соглашение
  </Link>
  <Link href="/legal/offer" className="hover:text-ocean-wave-start">
    Публичная оферта
  </Link>
  <Link href="/legal/cookies" className="hover:text-ocean-wave-start">
    Политика cookies
  </Link>
</div>
```

---

## 📚 Полезные ресурсы

- [Генератор политики конфиденциальности](https://www.privacypolicygenerator.info/)
- [GDPR Compliance Guide](https://gdpr.eu/)
- [ФЗ-152 о персональных данных](http://www.consultant.ru/document/cons_doc_LAW_61801/)

---

## ⚠️ Важно

1. **Юридическая проверка обязательна**
   - Все документы должны быть проверены юристом
   - Особенно политика конфиденциальности и оферта

2. **Регулярное обновление**
   - Проверять актуальность документов раз в год
   - Обновлять при изменении законодательства

3. **Версионирование**
   - Указывать версию и дату последнего обновления
   - Сохранять старые версии в Git

4. **Доступность**
   - Ссылки должны быть видны в Footer
   - Легко найти на сайте

