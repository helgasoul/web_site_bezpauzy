# Отчет о проверке RLS политик

**Дата создания:** 2025-01-09  
**Статус:** Требуется проверка в Supabase Dashboard

---

## 📋 Инструкция по проверке

1. Откройте **Supabase Dashboard** → SQL Editor
2. Скопируйте содержимое файла `supabase/migrations/051_check_rls_status.sql`
3. Выполните запрос
4. Проверьте результаты каждой секции

---

## ✅ Ожидаемое состояние таблиц

### Критичные таблицы (RLS обязателен)

| Таблица | RLS Статус | Ожидаемое кол-во политик | Описание |
|---------|------------|-------------------------|----------|
| `menohub_users` | ✅ ENABLED | 3+ | Service role для всех операций |
| `menohub_quiz_results` | ✅ ENABLED | 2+ | Service role + пользователи свои результаты |
| `menohub_user_saved_content` | ✅ ENABLED | 2+ | Service role + пользователи свой контент |
| `menohub_telegram_auth_codes` | ✅ ENABLED | 1+ | Только service role (чувствительные данные) |
| `menohub_newsletter_subscribers` | ✅ ENABLED | 2+ | Service role для всех операций |
| `menohub_book_orders` | ✅ ENABLED | 3+ | Service role для всех операций |
| `menohub_contact_submissions` | ✅ ENABLED | 3+ | Service role для всех операций |
| `menohub_resource_purchases` | ✅ ENABLED | 4+ | Service role для всех операций |
| `menohub_user_cookie_preferences` | ✅ ENABLED | 1+ | Service role для всех операций |
| `menohub_support_requests` | ✅ ENABLED | 3+ | Service role для всех операций |

### Публичные таблицы (публичное чтение)

| Таблица | RLS Статус | Ожидаемое | Описание |
|---------|------------|-----------|----------|
| `menohub_blog_posts` | ✅ ENABLED | Публичное чтение опубликованных | Service role для записи |
| `menohub_video_content` | ✅ ENABLED | Публичное чтение опубликованных | Service role для записи |
| `menohub_resources` | ✅ ENABLED | Публичное чтение опубликованных | Service role для записи |
| `menohub_experts` | ✅ ENABLED | Публичное чтение для всех | Service role для записи |
| `menohub_testimonials` | ✅ ENABLED | Публичное чтение одобренных | Пользователи могут создавать |

### Таблицы форума

| Таблица | RLS Статус | Ожидаемое |
|---------|------------|-----------|
| `menohub_forum_categories` | ✅ ENABLED | Публичное чтение |
| `menohub_forum_topics` | ✅ ENABLED | Публичное чтение, service role для записи |
| `menohub_forum_replies` | ✅ ENABLED | Публичное чтение, service role для записи |

### Другие таблицы

| Таблица | RLS Статус | Ожидаемое |
|---------|------------|-----------|
| `menohub_community_members` | ✅ ENABLED | Service role для всех операций |
| `menohub_user_cart` | ✅ ENABLED | Пользователи свой cart |
| `feature_votes` | ✅ ENABLED | Пользователи свои votes |
| `platform_events` | ✅ ENABLED | Публичное чтение опубликованных |
| `menohub_page_views` | ✅ ENABLED | Service role для всех операций |
| `menohub_test_results` | ✅ ENABLED | Service role для всех операций |

### Таблицы бота (исправлено в миграции 052)

| Таблица | RLS Статус | Ожидаемое | Статус |
|---------|------------|-----------|--------|
| `menohub_chat_logs` | ✅ ENABLED | Service role для всех операций | ✅ Исправлено |
| `menohub_documents` | ✅ ENABLED | Service role для всех операций | ✅ Исправлено |
| `menohub_queries` | ✅ ENABLED | Service role для всех операций | ✅ Исправлено |

---

## 🔍 Миграции, которые настраивают RLS

### Основные миграции RLS:

1. **`009_add_menohub_users_rls_policies.sql`** - Политики для пользователей
2. **`022_improve_rls_policies.sql`** - Улучшенные политики для критичных таблиц
3. **`019_create_website_tables.sql`** - Политики для newsletter, book_orders, contact_submissions
4. **`002_create_community_forum.sql`** - Политики для форума
5. **`025_create_resources_table.sql`** - Политики для ресурсов
6. **`027_create_resource_purchases_table.sql`** - Политики для покупок
7. **`020_create_blog_posts_table.sql`** - Политики для блога
8. **`021_create_video_content_table.sql`** - Политики для видео
9. **`052_add_rls_policies_for_missing_tables.sql`** - ⚠️ **КРИТИЧНО:** Политики для таблиц без политик (chat_logs, documents, queries)

---

## ⚠️ Типичные проблемы

### 1. RLS включен, но нет политик
**Проблема:** Таблица блокирует все операции  
**Решение:** Создать политики через `CREATE POLICY`

### 2. RLS не включен
**Проблема:** Таблица доступна всем  
**Решение:** Выполнить `ALTER TABLE table_name ENABLE ROW LEVEL SECURITY;`

### 3. Слишком разрешительные политики
**Проблема:** Политика `WITH CHECK (true)` без проверки role  
**Решение:** Использовать `auth.role() = 'service_role'` или ограничить доступ

---

## 🔧 Быстрый фикс проблем

Если найдена таблица без RLS или политик:

```sql
-- 1. Включить RLS
ALTER TABLE table_name ENABLE ROW LEVEL SECURITY;

-- 2. Создать минимальную политику для service role
CREATE POLICY "Service role can manage table_name"
    ON table_name
    FOR ALL
    USING (auth.role() = 'service_role')
    WITH CHECK (auth.role() = 'service_role');
```

---

## 📝 Чеклист проверки

- [ ] Запустить скрипт `051_check_rls_status.sql` в Supabase
- [ ] Проверить, что все критичные таблицы имеют RLS включен
- [ ] Проверить, что на всех таблицах есть политики
- [ ] Проверить, что публичные таблицы имеют политику публичного чтения
- [ ] Убедиться, что нет таблиц с RLS, но без политик
- [ ] Проверить политики форума
- [ ] Убедиться, что service role имеет доступ ко всем нужным таблицам

---

## 📊 Пример результата успешной проверки

```
table_name                          | rls_status    | policy_count
------------------------------------|---------------|-------------
menohub_users                       | ✅ ENABLED    | 3
menohub_quiz_results                | ✅ ENABLED    | 2
menohub_blog_posts                  | ✅ ENABLED    | 5
menohub_book_orders                 | ✅ ENABLED    | 3
...
```

Все таблицы должны иметь `✅ ENABLED` и `policy_count > 0`.
