# Настройка поддомена для staging (бесплатно)

## ✅ Хорошая новость: Поддомен НЕ нужно покупать!

**Поддомен создается бесплатно** через DNS настройки вашего основного домена. Вы просто добавляете DNS запись, и поддомен автоматически работает.

## 🔧 Как настроить поддомен

### Шаг 1: В панели Vercel

1. Откройте ваш проект в Vercel
2. Перейдите в **Settings** → **Domains**
3. Нажмите **Add Domain**
4. Введите поддомен: `staging.bezpauzy.com` (или `dev.bezpauzy.com`)
5. Vercel покажет вам инструкции по настройке DNS

### Шаг 2: В панели регистратора домена

**Где настраивать**: В панели управления доменом у вашего регистратора (где вы покупали домен)

**Что добавить**: DNS запись типа CNAME

**Пример для разных регистраторов**:

#### Если домен на Namecheap:
1. Зайдите в **Domain List** → выберите ваш домен
2. Перейдите в **Advanced DNS**
3. Нажмите **Add New Record**
4. Выберите тип: **CNAME Record**
5. Заполните:
   - **Host**: `staging` (или `dev`)
   - **Value**: `cname.vercel-dns.com`
   - **TTL**: `Automatic` или `3600`
6. Сохраните

#### Если домен на GoDaddy:
1. Зайдите в **My Products** → **DNS**
2. Нажмите **Add** в разделе Records
3. Выберите тип: **CNAME**
4. Заполните:
   - **Name**: `staging` (или `dev`)
   - **Value**: `cname.vercel-dns.com`
   - **TTL**: `1 hour`
5. Сохраните

#### Если домен на REG.RU, Timeweb, или другом российском регистраторе:
1. Зайдите в панель управления доменом
2. Найдите раздел **DNS записи** или **Управление DNS**
3. Добавьте новую запись:
   - **Тип**: CNAME
   - **Имя/Поддомен**: `staging` (или `dev`)
   - **Значение**: `cname.vercel-dns.com`
   - **TTL**: `3600`
4. Сохраните

### Шаг 3: Ожидание

После добавления DNS записи:
- ⏱️ Обычно занимает **5-30 минут** (иногда до 24 часов)
- ✅ Vercel автоматически проверит и активирует домен
- ✅ SSL сертификат выдается автоматически

## 📋 Пример DNS записей

После настройки у вас будет:

```
Основной домен:
bezpauzy.com → A record → IP Vercel (когда будет готово)

Поддомен staging:
staging.bezpauzy.com → CNAME → cname.vercel-dns.com
```

## 🎯 Рекомендуемые поддомены

Вы можете создать несколько поддоменов бесплатно:

- `staging.bezpauzy.com` - для тестирования
- `dev.bezpauzy.com` - для разработки
- `test.bezpauzy.com` - для тестов
- `preview.bezpauzy.com` - для превью

**Все они бесплатны!** Просто добавьте CNAME записи.

## ✅ Чеклист настройки

- [ ] Добавили поддомен в Vercel (Settings → Domains → Add Domain)
- [ ] Скопировали значение для CNAME записи из Vercel
- [ ] Зашли в панель регистратора домена
- [ ] Добавили CNAME запись:
  - Имя: `staging` (или другое)
  - Значение: `cname.vercel-dns.com`
- [ ] Сохранили изменения
- [ ] Подождали 5-30 минут
- [ ] Проверили, что домен активирован в Vercel

## 🔍 Проверка работы

После настройки проверьте:

1. **В Vercel**: Домен должен показать статус "Valid Configuration" ✅
2. **В браузере**: Откройте `https://staging.bezpauzy.com` - должен открыться ваш сайт
3. **SSL**: Должен автоматически работать HTTPS

## ⚠️ Важные моменты

1. **Не нужно покупать** поддомен отдельно - он бесплатный
2. **DNS изменения** могут занять время (до 24 часов, обычно 5-30 минут)
3. **SSL сертификат** выдается автоматически Vercel (Let's Encrypt)
4. **Можно создать** сколько угодно поддоменов бесплатно

## 🆘 Если не работает

1. **Проверьте DNS запись**:
   - Убедитесь, что тип записи: **CNAME** (не A, не AAAA)
   - Значение должно быть: `cname.vercel-dns.com` (точка в конце не нужна)

2. **Проверьте имя поддомена**:
   - В DNS: `staging` (без точки, без домена)
   - В Vercel: `staging.bezpauzy.com` (полное имя)

3. **Подождите**:
   - DNS изменения могут занять время
   - Проверьте через 30 минут

4. **Проверьте в Vercel**:
   - Settings → Domains → посмотрите статус
   - Если есть ошибка, Vercel покажет что не так

## 📝 Пример полной настройки

```
1. В Vercel:
   Settings → Domains → Add Domain
   Введите: staging.bezpauzy.com
   Скопируйте значение для CNAME

2. В панели регистратора:
   Добавьте CNAME запись:
   Имя: staging
   Значение: cname.vercel-dns.com
   TTL: 3600

3. Подождите 5-30 минут

4. Проверьте:
   https://staging.bezpauzy.com
```

## 🎉 Итог

**Поддомен = бесплатно!** Просто добавьте CNAME запись в DNS настройках вашего домена.

