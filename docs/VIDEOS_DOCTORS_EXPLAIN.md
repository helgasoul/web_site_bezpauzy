# Раздел "Врачи Объясняют" - Документация

## 📋 Обзор

Новый раздел для экспертных видео от врачей-специалистов о женском здоровье, менопаузе и качестве жизни.

---

## 🗄️ База данных

### Миграция

**Файл:** `supabase/migrations/055_add_doctors_explain_content_type.sql`

**Изменения:**
- Добавлен новый тип контента `'doctors_explain'`
- Добавлены поля:
  - `doctor_id` - ID врача (BIGINT)
  - `doctor_name` - Имя врача (TEXT) *обязательное*
  - `doctor_specialty` - Специализация (TEXT) *обязательное*
  - `doctor_credentials` - Регалии (TEXT)
  - `doctor_avatar` - URL фото врача (TEXT)
- Обновлен constraint для `video_type` (добавлены: mave, supabase)

**Применение миграции:**

```bash
# Через Supabase CLI
supabase db push

# Или через SQL Editor в Supabase Dashboard
# Скопируйте содержимое файла миграции и выполните
```

---

## 🎯 API Endpoints

### Admin API

#### GET `/api/admin/videos`
Получить список всех видео с фильтрами

**Query параметры:**
- `page` - номер страницы (по умолчанию: 1)
- `limit` - количество на странице (по умолчанию: 20)
- `search` - поиск по названию, автору, теме
- `contentType` - фильтр по типу (`podcast`, `eva_explains`, `doctors_explain`)
- `category` - фильтр по категории
- `published` - статус публикации (`true`, `false`)
- `sortBy` - поле сортировки (по умолчанию: `created_at`)
- `sortOrder` - направление (`asc`, `desc`)

**Доступ:** `super_admin`, `content_manager`

#### POST `/api/admin/videos`
Создать новое видео

**Body:** JSON с полями видео (см. VideoFormData)

**Доступ:** `super_admin`, `content_manager`

#### GET `/api/admin/videos/[id]`
Получить видео по ID

**Доступ:** `super_admin`, `content_manager`

#### PATCH `/api/admin/videos/[id]`
Обновить видео

**Body:** JSON с полями для обновления

**Доступ:** `super_admin`, `content_manager`

#### DELETE `/api/admin/videos/[id]`
Удалить видео

**Доступ:** только `super_admin`

---

## 🖥️ Админ-панель

### Страницы

#### `/admin/videos`
Список всех видео контента

**Функционал:**
- Таблица с миниатюрами
- Фильтры по типу контента, статусу публикации
- Поиск по названию, автору, теме
- Отображение типа контента с цветными бейджами:
  - 🟣 Фиолетовый - Подкаст
  - 🩷 Розовый - Ева объясняет
  - 🩵 Бирюзовый - Врачи объясняют
- Информация об авторе (эксперт/доктор/тема)
- Просмотры и дата публикации

#### `/admin/videos/create`
Создание нового видео

**Функционал:**
- Динамическая форма (поля меняются в зависимости от типа контента)
- Автогенерация slug из названия
- Автоматическое извлечение YouTube ID
- Автозаполнение миниатюры для YouTube
- Превью миниатюры
- SEO поля
- Управление тегами
- Выбор категории и уровня доступа

#### `/admin/videos/[id]`
Редактирование существующего видео

**Функционал:**
- Все функции формы создания
- Кнопка "Просмотр" для опубликованных видео
- Сохранение изменений

### Навигация

В боковом меню админ-панели добавлен пункт:
- **Видео** (иконка FileVideo) - доступ: `super_admin`, `content_manager`

---

## 🌐 Публичные страницы

### `/videos/doctors-explain`
Список видео "Врачи Объясняют"

**Дизайн:**
- Hero секция с заголовком и описанием
- Сетка видео карточек (3 колонки на десктопе)
- Каждая карточка содержит:
  - Миниатюру с overlay play button при hover
  - Длительность
  - Категорию
  - Название
  - Информацию о враче (фото, имя, специализация)
  - Краткое описание
  - Просмотры и дату

### `/videos/doctors-explain/[slug]`
Страница отдельного видео

**Секции:**
1. **Кнопка "Назад"** - возврат к списку
2. **Видео плеер** - YouTube/Vimeo/Direct embed
3. **Информация о видео**:
   - Категория, название
   - Просмотры, длительность, дата
4. **Блок врача**:
   - Фото/аватар
   - Имя
   - Специализация
   - Регалии
5. **Описание** - подробное описание видео
6. **Расшифровка** (если есть) - полный текст
7. **Теги** - хештеги видео
8. **Sidebar**:
   - Кнопка "Поделиться"
   - Похожие видео (по категории)

---

## 📝 Типы данных

### VideoFormData (для админки)

```typescript
interface VideoFormData {
  // Общие поля
  title: string
  slug: string
  description: string
  content_type: 'podcast' | 'eva_explains' | 'doctors_explain'
  video_url: string
  video_type: 'youtube' | 'mave' | 'vimeo' | 'direct'
  video_id: string
  thumbnail_url: string
  duration: number // секунды
  category: string
  category_name: string
  access_level: 'free' | 'paid1' | 'paid2'
  published: boolean
  tags: string[]
  meta_title: string
  meta_description: string

  // Поля подкаста
  podcast_series?: string
  guest_expert_name?: string
  guest_expert_role?: string
  guest_expert_avatar?: string
  host_name?: string

  // Поля "Ева объясняет"
  topic?: string

  // Поля "Врачи объясняют"
  doctor_name?: string
  doctor_specialty?: string
  doctor_credentials?: string
  doctor_avatar?: string
}
```

### Категории

```typescript
const CATEGORIES = [
  { value: 'menopause', label: 'Менопауза' },
  { value: 'hormones', label: 'Гормоны и ЗГТ' },
  { value: 'nutrition', label: 'Питание' },
  { value: 'sports', label: 'Спорт и фитнес' },
  { value: 'mental_health', label: 'Психоэмоциональное здоровье' },
  { value: 'sexual_health', label: 'Сексуальное здоровье' },
  { value: 'bone_health', label: 'Здоровье костей' },
  { value: 'heart_health', label: 'Сердечно-сосудистое здоровье' },
  { value: 'sleep', label: 'Сон' },
  { value: 'skin_health', label: 'Кожа и волосы' },
  { value: 'general', label: 'Общее' },
]
```

---

## 🚀 Как добавить видео

### Через админ-панель

1. **Войдите в админку** → `/admin/login`
2. **Перейдите в "Видео"** → `/admin/videos`
3. **Нажмите "Добавить видео"**
4. **Заполните форму:**

   **Основная информация:**
   - Название (обязательно)
   - Slug (автогенерируется, можно редактировать)
   - Описание (обязательно)

   **Видео:**
   - URL видео (обязательно) - вставьте YouTube ссылку
   - Тип видео (автоопределяется для YouTube)
   - Video ID (автозаполняется для YouTube)
   - URL миниатюры (автозаполняется для YouTube)
   - Длительность в секундах

   **Для "Врачи объясняют":**
   - Имя врача (обязательно)
   - Специализация (обязательно)
   - Регалии (опционально)
   - Фото врача URL (опционально)

   **Публикация:**
   - Тип контента: "Врачи объясняют"
   - Категория
   - Уровень доступа (free/paid1/paid2)
   - Галочка "Опубликовать"

   **Теги:**
   - Введите через запятую

   **SEO (опционально):**
   - Meta Title
   - Meta Description

5. **Нажмите "Сохранить"**

### Пример добавления YouTube видео

1. URL видео: `https://www.youtube.com/watch?v=dQw4w9WgXcQ`
2. Система автоматически:
   - Определит video_type = `youtube`
   - Извлечет video_id = `dQw4w9WgXcQ`
   - Установит thumbnail_url = `https://img.youtube.com/vi/dQw4w9WgXcQ/maxresdefault.jpg`

---

## ✅ Чеклист внедрения

- [x] Миграция базы данных создана
- [x] TypeScript типы обновлены
- [x] API endpoints созданы
- [x] Админ-панель: таблица видео
- [x] Админ-панель: форма создания
- [x] Админ-панель: форма редактирования
- [x] Админ-панель: навигация обновлена
- [x] Публичная страница: список видео
- [x] Публичная страница: страница видео
- [ ] Применить миграцию к БД
- [ ] Добавить ссылку в главную навигацию сайта
- [ ] Протестировать создание видео
- [ ] Добавить несколько тестовых видео

---

## 🔧 Что делать дальше

1. **Применить миграцию:**
   ```bash
   # В Supabase SQL Editor выполните:
   # содержимое файла supabase/migrations/055_add_doctors_explain_content_type.sql
   ```

2. **Добавить ссылку в навигацию:**
   - Обновите главное меню сайта
   - Добавьте ссылку `/videos/doctors-explain`

3. **Протестировать:**
   - Создайте тестовое видео через админку
   - Проверьте отображение на публичной странице

4. **Добавить контент:**
   - Загрузите реальные видео с врачами
   - Заполните все необходимые поля

---

## 🎨 Цветовая схема

Раздел "Врачи Объясняют" использует **бирюзовую** (teal/cyan) цветовую палитру:

- Primary: `teal-600`, `cyan-600`
- Gradient: `from-teal-600 to-cyan-600`
- Background: `teal-50`, `cyan-50`
- Badges: `teal-100`, `teal-800`

---

## 📞 Поддержка

При возникновении вопросов или проблем:
1. Проверьте логи сервера
2. Убедитесь, что миграция применена
3. Проверьте права доступа админа (должна быть роль `content_manager` или `super_admin`)
