# Архитектура интеграции чата на сайте с Telegram ботом

## 📋 Текущая ситуация

### Что есть сейчас:

1. **Сайт** (`app/api/chat/send-message/route.ts`):
   - Сохраняет запрос в `menohub_queries` со статусом `processing`
   - Пытается отправить в n8n webhook (если настроен `N8N_WEBHOOK_URL`)
   - Если webhook не работает, возвращает временное сообщение

2. **Polling** (`app/api/chat/poll/route.ts`):
   - Проверяет обновления в БД каждые 2 секунды
   - Если `query_status === 'completed'`, возвращает ответ

3. **История** (`app/api/chat/history/route.ts`):
   - Получает все запросы из `menohub_queries` по `user_id`

### Проблема:

- Нет полной интеграции с n8n
- Webhook может не работать
- Нет гарантии, что ответ от бота попадет в БД

---

## 🏗️ Варианты архитектуры

### **Вариант 1: Через БД (рекомендуемый) ✅**

**Принцип работы:**

```
Сайт → БД → n8n (polling) → Telegram Bot → n8n → БД → Сайт (polling)
```

**Детали:**

1. **Сайт отправляет запрос:**
   - Сохраняет в `menohub_queries`:
     ```sql
     INSERT INTO menohub_queries (
       user_id,
       query_text,
       query_status,  -- 'pending'
       response_text, -- NULL
       source        -- 'website'
     )
     ```

2. **n8n проверяет новые запросы:**
   - Узел "Cron" или "Interval" каждые 5-10 секунд
   - Запрос к БД: `SELECT * FROM menohub_queries WHERE query_status = 'pending' AND source = 'website'`
   - Обновляет статус на `processing`

3. **n8n отправляет в Telegram:**
   - Использует `telegram_id` из `menohub_users`
   - Отправляет сообщение через Telegram Bot API
   - Сохраняет `telegram_message_id` в `menohub_queries`

4. **n8n получает ответ от бота:**
   - Webhook от Telegram получает ответ
   - Находит запрос по `telegram_message_id` или `user_id`
   - Обновляет БД:
     ```sql
     UPDATE menohub_queries
     SET 
       response_text = 'ответ от бота',
       query_status = 'completed',
       updated_at = NOW()
     WHERE id = query_id
     ```

5. **Сайт получает ответ:**
   - Polling проверяет обновления
   - Показывает ответ пользователю

**Преимущества:**
- ✅ Надежно (БД как единый источник истины)
- ✅ Работает даже если n8n временно недоступен
- ✅ Легко отслеживать статусы
- ✅ Можно добавить retry логику

**Недостатки:**
- ⚠️ Небольшая задержка (polling каждые 5-10 секунд)
- ⚠️ Нужно настроить polling в n8n

---

### **Вариант 2: Webhook от сайта к n8n**

**Принцип работы:**

```
Сайт → n8n webhook → Telegram Bot → n8n webhook → Сайт API → БД
```

**Детали:**

1. **Сайт отправляет запрос:**
   - Сохраняет в БД со статусом `processing`
   - Отправляет POST запрос в n8n webhook:
     ```json
     {
       "message": "текст вопроса",
       "telegram_id": 123456789,
       "user_id": 111,
       "query_id": "uuid-запроса",
       "source": "website"
     }
     ```

2. **n8n обрабатывает:**
   - Получает webhook
   - Отправляет в Telegram бот
   - Ждет ответ от бота (через webhook от Telegram)

3. **n8n отправляет ответ обратно:**
   - POST запрос на сайт: `https://bezpauzy.com/api/chat/webhook-response`
   - Сайт обновляет БД

**Преимущества:**
- ✅ Быстрее (нет polling)
- ✅ Real-time обновления

**Недостатки:**
- ⚠️ Нужен публичный URL для webhook
- ⚠️ Нужна аутентификация webhook
- ⚠️ Может не работать при проблемах с сетью

---

### **Вариант 3: Гибридный (рекомендуемый для production)**

**Принцип работы:**

```
Сайт → БД + n8n webhook (async) → Telegram Bot → n8n → БД → Сайт (polling)
```

**Детали:**

1. **Сайт отправляет запрос:**
   - Сохраняет в БД со статусом `pending`
   - Параллельно отправляет webhook в n8n (не ждет ответа)
   - Если webhook не работает, n8n все равно получит через polling

2. **n8n обрабатывает:**
   - Приоритет: webhook (быстрее)
   - Fallback: polling (надежнее)

3. **Остальное как в Варианте 1**

**Преимущества:**
- ✅ Быстро (webhook) + надежно (polling fallback)
- ✅ Лучшее из обоих миров

---

## 🎯 Рекомендация: Вариант 1 (через БД)

### Почему:

1. **Простота реализации:**
   - Не нужен публичный URL
   - Не нужна аутентификация webhook
   - Легко отлаживать

2. **Надежность:**
   - БД как единый источник истины
   - Работает даже при проблемах с сетью
   - Можно добавить retry логику

3. **Масштабируемость:**
   - Легко добавить несколько n8n инстансов
   - Легко отслеживать статусы
   - Легко добавить логирование

---

## 📝 Что нужно сделать

### 1. Обновить структуру БД (если нужно)

```sql
-- Убедиться, что есть поля:
ALTER TABLE menohub_queries
  ADD COLUMN IF NOT EXISTS source TEXT DEFAULT 'telegram', -- 'website' или 'telegram'
  ADD COLUMN IF NOT EXISTS telegram_message_id BIGINT,
  ADD COLUMN IF NOT EXISTS query_status TEXT DEFAULT 'pending'; -- 'pending', 'processing', 'completed', 'failed'
```

### 2. Обновить API на сайте

**`app/api/chat/send-message/route.ts`:**
- Сохранять запрос со статусом `pending` и `source = 'website'`
- Не пытаться отправлять webhook (или делать это асинхронно)

### 3. Настроить n8n workflow

**Новый workflow "Website Chat Handler":**

1. **Узел "Cron"** (каждые 5-10 секунд):
   - Проверяет новые запросы в БД

2. **Узел "Get pending queries"** (Supabase):
   ```sql
   SELECT * FROM menohub_queries
   WHERE query_status = 'pending'
     AND source = 'website'
   ORDER BY created_at ASC
   LIMIT 10
   ```

3. **Узел "Update status to processing"**:
   - Обновляет статус на `processing`

4. **Узел "Get user telegram_id"**:
   - Получает `telegram_id` из `menohub_users` по `user_id`

5. **Узел "Send to Telegram"**:
   - Отправляет сообщение через Telegram Bot API
   - Сохраняет `telegram_message_id`

6. **Узел "Wait for response"**:
   - Ждет ответ от бота (через webhook от Telegram)

7. **Узел "Update DB with response"**:
   - Обновляет `menohub_queries`:
     ```sql
     UPDATE menohub_queries
     SET 
       response_text = 'ответ',
       query_status = 'completed',
       updated_at = NOW()
     WHERE id = query_id
     ```

### 4. Обновить существующий Telegram webhook

**В существующем workflow "БЕЗ_ПАУЗЫ основной сценарий":**

- Добавить проверку: если запрос пришел из Telegram, обрабатывать как обычно
- Если запрос пришел из сайта (есть `query_id` в БД), обновлять БД вместо отправки в Telegram

---

## 🔄 Flow диаграмма (Вариант 1)

```
┌─────────┐
│  Сайт   │
│ (чат)   │
└────┬────┘
     │ 1. POST /api/chat/send-message
     │    { message, userId }
     ▼
┌─────────┐
│   БД    │
│ queries │ INSERT: status='pending', source='website'
└────┬────┘
     │ 2. Polling каждые 5-10 сек
     ▼
┌─────────┐
│  n8n    │ SELECT * WHERE status='pending' AND source='website'
│ workflow│
└────┬────┘
     │ 3. UPDATE status='processing'
     │ 4. GET telegram_id FROM users
     │ 5. Send to Telegram Bot
     ▼
┌─────────┐
│Telegram │
│  Bot    │
└────┬────┘
     │ 6. Webhook с ответом
     ▼
┌─────────┐
│  n8n    │ UPDATE queries SET response_text=..., status='completed'
│ workflow│
└────┬────┘
     │ 7. БД обновлена
     ▼
┌─────────┐
│  Сайт   │ Polling проверяет обновления
│ (чат)   │ Показывает ответ пользователю
└─────────┘
```

---

## 🚀 Следующие шаги

1. **Обсудить с пользователем:**
   - Какой вариант предпочтительнее?
   - Есть ли уже настроенный n8n webhook?
   - Какие требования к скорости ответа?

2. **Реализовать выбранный вариант:**
   - Обновить БД (если нужно)
   - Обновить API на сайте
   - Настроить n8n workflow

3. **Протестировать:**
   - Отправить запрос с сайта
   - Проверить, что он попадает в БД
   - Проверить, что n8n его обрабатывает
   - Проверить, что ответ возвращается на сайт

---

## ❓ Вопросы для обсуждения

1. **Какой вариант предпочтительнее?**
   - Вариант 1 (через БД) - проще и надежнее
   - Вариант 2 (webhook) - быстрее, но сложнее
   - Вариант 3 (гибридный) - лучшее из обоих

2. **Есть ли уже настроенный n8n webhook?**
   - Если да, можно использовать его
   - Если нет, лучше начать с Варианта 1

3. **Какие требования к скорости ответа?**
   - Если нужен real-time, лучше webhook
   - Если задержка 5-10 секунд приемлема, polling достаточно

4. **Как обрабатывать ошибки?**
   - Retry логика в n8n?
   - Уведомления пользователю?
   - Логирование?

