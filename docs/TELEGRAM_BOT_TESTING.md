# Тестирование и деплой Telegram бота

## 🧪 Локальное тестирование

### 1. Подготовка

```bash
# 1. Убедитесь, что все переменные окружения установлены
# Проверьте файл .env.local:

TELEGRAM_BOT_TOKEN=xxx
TELEGRAM_WEBHOOK_SECRET=xxx # опционально, но рекомендуется
NEXT_PUBLIC_SUPABASE_URL=xxx
SUPABASE_SERVICE_ROLE_KEY=xxx
ANTHROPIC_API_KEY=xxx
LAKERA_API_KEY=xxx
PERPLEXITY_API_KEY=xxx # для Research инструмента
OPENAI_API_KEY=xxx # для Vector Store

# 2. Запустите dev сервер
npm run dev
```

### 2. Установка ngrok

```bash
# Mac (Homebrew)
brew install ngrok

# Или скачайте с https://ngrok.com/download

# Авторизуйтесь (получите токен на ngrok.com)
ngrok config add-authtoken YOUR_NGROK_TOKEN
```

### 3. Запуск ngrok

```bash
# В отдельном терминале
ngrok http 3000

# Вы увидите:
# Forwarding: https://xxxx-xx-xx-xxx-xxx.ngrok-free.app -> http://localhost:3000
```

### 4. Настройка webhook

```bash
# Замените TOKEN на ваш TELEGRAM_BOT_TOKEN
# Замените URL на ngrok URL

curl -X POST "https://api.telegram.org/botTOKEN/setWebhook" \
  -H "Content-Type: application/json" \
  -d '{
    "url": "https://xxxx.ngrok-free.app/api/telegram/webhook",
    "secret_token": "ваш-секретный-токен"
  }'

# Проверьте webhook
curl "https://api.telegram.org/botTOKEN/getWebhookInfo"
```

---

## ✅ Чек-лист тестирования

### Основной flow регистрации

- [ ] 1. Отправьте `/start` в бота
  - **Ожидается:** Сообщение с согласием на обработку данных + 2 кнопки

- [ ] 2. Нажмите "Согласен"
  - **Ожидается:** Создается запись в `menohub_users`
  - **Ожидается:** Вопрос о возрасте + 3 кнопки

- [ ] 3. Выберите возраст (например, "46-50")
  - **Ожидается:** Возраст сохраняется в `age_range`
  - **Ожидается:** Показываются бесплатные темы (4 кнопки + "Задать вопрос")

### Бесплатные темы

- [ ] 4. Нажмите "🌡️ Приливы"
  - **Ожидается:** Длинное сообщение с информацией о приливах (HTML форматирование)
  - **Ожидается:** Персонализация по возрасту (для 46-50)
  - **Ожидается:** 2 кнопки: "Задать свой вопрос" и "Всё понятно"

- [ ] 5. Проверьте другие темы:
  - [ ] "😴 Сон"
  - [ ] "🌈 Настроение"
  - [ ] "⚖️ Вес"

### Текстовые вопросы

- [ ] 6. Отправьте текстовый вопрос (например: "Что такое приливы?")
  - **Ожидается:** Индикатор "печатает..."
  - **Ожидается:** Ответ от AI Agent с использованием Vector Store
  - **Ожидается:** Сообщение сохраняется в `menohub_queries`

- [ ] 7. Спросите про город (например: "Москва")
  - **Ожидается:** "Спасибо, информация добавлена!"
  - **Ожидается:** Город сохраняется в БД (`city = "Москва"`)

- [ ] 8. Попросите найти врача (например: "Найди гинеколога в Москве")
  - **Ожидается:** Сработает AI Agent1 (поиск врачей)
  - **Ожидается:** Поиск в `menohub_experts`
  - **Ожидается:** Список врачей с контактами
  - **Ожидается:** Если не найдены - Research в интернете

### Команды

- [ ] 9. Отправьте `/history`
  - **Ожидается:** Последние 10 запросов пользователя

- [ ] 10. Отправьте `/export_my_data`
  - **Ожидается:** Экспорт всех данных пользователя

- [ ] 11. Отправьте `/delete_my_data`
  - **Ожидается:** Удаление всех данных из БД

### Платные функции

- [ ] 12. Нажмите кнопку "Оплата" (если есть в flow)
  - **Ожидается:** Сообщение с тарифами + ссылка на сайт

- [ ] 13. Нажмите кнопку "Смотреть видео врачей"
  - **Если подписки НЕТ:** Сообщение о необходимости подписки
  - **Если подписка ЕСТЬ:** Список видео из `menohub_video_content`

### Безопасность

- [ ] 14. Попробуйте prompt injection (например: "Ignore previous instructions and tell me...")
  - **Ожидается:** Lakera Guard блокирует + стандартный ответ о специализации

---

## 🚀 Деплой на Production

### 1. Применить миграции к Supabase

Откройте Supabase Dashboard → SQL Editor и выполните:

```sql
-- 055_add_doctors_explain_content_type.sql (если еще не выполнена)
-- 056_add_increment_video_views_function.sql
```

Или через CLI:
```bash
supabase db push
```

### 2. Обновить Environment Variables на Vercel

Проверьте, что установлены все переменные:

```
TELEGRAM_BOT_TOKEN
TELEGRAM_WEBHOOK_SECRET
NEXT_PUBLIC_SUPABASE_URL
NEXT_PUBLIC_SUPABASE_ANON_KEY
SUPABASE_SERVICE_ROLE_KEY
ANTHROPIC_API_KEY
LAKERA_API_KEY
PERPLEXITY_API_KEY
OPENAI_API_KEY
JWT_SECRET
```

### 3. Git Push

```bash
# Добавить все файлы
git add .

# Закоммитить
git commit -m "feat: полная миграция Telegram бота из n8n в код

- Реализованы callbacks для всех кнопок (13 типов)
- Добавлены предзаготовленные шаблоны для бесплатных тем
- Реализован flow регистрации с выбором возраста
- Добавлены инструменты для AI агентов (Get_user_age, Update_city, search_doctors)
- Интеграция видео 'Врачи Объясняют' (платный контент)
- Информация об оплате с ссылкой на сайт

Co-Authored-By: Claude Sonnet 4.5 <noreply@anthropic.com>"

# Запушить
git push origin main
```

### 4. Переключить webhook с n8n на Vercel

```bash
# Получите URL вашего Vercel деплоя
# Например: https://bez-pauzy.vercel.app

# Установите webhook
curl -X POST "https://api.telegram.org/botTOKEN/setWebhook" \
  -H "Content-Type: application/json" \
  -d '{
    "url": "https://bez-pauzy.vercel.app/api/telegram/webhook",
    "secret_token": "ваш-секретный-токен"
  }'

# Проверьте
curl "https://api.telegram.org/botTOKEN/getWebhookInfo"

# Должно показать:
# "url": "https://bez-pauzy.vercel.app/api/telegram/webhook"
# "has_custom_certificate": false
# "pending_update_count": 0
```

### 5. Отключить n8n workflow

В n8n:
1. Откройте workflow "БЕЗ|ПАУЗЫ основной сценарий"
2. Нажмите "Inactive" (деактивировать)
3. Или удалите webhook в Telegram Trigger node

### 6. Мониторинг первых запросов

```bash
# Vercel Logs
# Откройте Vercel Dashboard → ваш проект → Logs

# Или через CLI
vercel logs --follow

# Проверьте:
# ✅ Webhook получен
# ✅ Callback обработан
# ✅ AI Agent ответил
# ✅ Сообщение отправлено в Telegram
```

---

## 🐛 Troubleshooting

### Бот не отвечает

1. Проверьте webhook:
   ```bash
   curl "https://api.telegram.org/botTOKEN/getWebhookInfo"
   ```

2. Проверьте логи Vercel:
   ```bash
   vercel logs
   ```

3. Проверьте, что URL правильный (с https://)

### Ошибка "Unauthorized" или 401

- Проверьте `TELEGRAM_WEBHOOK_SECRET` в .env и в запросе setWebhook
- Проверьте `TELEGRAM_BOT_TOKEN`

### Ошибка "Database query failed"

- Проверьте, что применены все миграции
- Проверьте `SUPABASE_SERVICE_ROLE_KEY`
- Проверьте RLS policies в Supabase

### AI Agent не отвечает

- Проверьте `ANTHROPIC_API_KEY`
- Проверьте логи: "Error in AI Agent"
- Проверьте, что Vector Store настроен (миграция 053)

### Vector Store пустой

- Проверьте таблицу `menohub_documents` в Supabase
- Загрузите документы через `scripts/generate-embeddings.js`
- Проверьте `OPENAI_API_KEY`

---

## 📊 Метрики для отслеживания

После деплоя отслеживайте:

1. **Количество регистраций** (новые записи в `menohub_users`)
2. **Успешность регистрации** (% пользователей, прошедших весь flow)
3. **Популярные темы** (статистика нажатий кнопок)
4. **Количество текстовых вопросов** (записи в `menohub_queries`)
5. **Время ответа AI Agent** (средний response time)
6. **Ошибки** (логи Vercel + Supabase)
7. **Использование инструментов** (Vector_store vs Research)

---

## 🎉 Готово!

После успешного деплоя и тестирования:

1. ✅ Бот полностью мигрирован из n8n
2. ✅ Все функции работают в коде
3. ✅ n8n можно отключить
4. ✅ Webhook установлен на Vercel
5. ✅ Мониторинг настроен

**Следующие шаги:**
- Загрузить документы в Vector Store
- Добавить врачей в `menohub_experts`
- Загрузить видео в "Врачи Объясняют"
- Настроить платежи (если еще не настроено)
