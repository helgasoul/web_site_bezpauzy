# Инструкция по установке и запуску

## Шаг 1: Установка зависимостей

```bash
npm install
```

## Шаг 2: Настройка переменных окружения

Создайте файл `.env.local` в корне проекта:

```env
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
```

📖 **Подробная инструкция по подключению Supabase**: см. файл `SUPABASE_SETUP.md`

**Если Supabase развернут на Beget**: см. файл `SUPABASE_BEGET_QUICKSTART.md` для быстрого старта

**Быстрый старт (облачный Supabase):**
1. Создайте проект на [supabase.com](https://supabase.com)
2. Получите URL и anon key в Settings → API
3. Создайте `.env.local` с этими значениями
4. Выполните SQL миграцию из `supabase/migrations/001_create_community_members.sql` в Supabase Dashboard → SQL Editor

**Быстрый старт (Supabase на Beget):**
1. Получите URL и anon key у администратора Beget
2. Создайте `.env.local` с этими значениями
3. Выполните SQL миграцию через Dashboard или SSH (см. `SUPABASE_BEGET_QUICKSTART.md`)

> **Примечание**: Если у вас еще нет Supabase проекта, вы можете запустить сайт без него для просмотра дизайна. Supabase нужен для функциональности (блог, пользователи, сообщество и т.д.).

## Шаг 3: Запуск dev-сервера

```bash
npm run dev
```

Откройте [http://localhost:3000](http://localhost:3000) в браузере.

## Что уже готово

✅ Базовая структура Next.js проекта  
✅ Дизайн-система (цвета, типографика, spacing)  
✅ Главная страница со всеми секциями:
- Hero секция
- Social Proof
- Problem/Solution
- Latest Articles
- Book Teaser
- How It Works
- Testimonials
- CTA Section
✅ Header и Footer  
✅ Компонент Button  
✅ Интеграция с Supabase (базовая)

## Следующие шаги

1. **Настроить Supabase** (если еще не настроен)
2. **Добавить контент** в компоненты (заменить placeholder данные)
3. **Создать страницы блога** (`/blog`, `/blog/[slug]`)
4. **Создать страницу книги** (`/book`)
5. **Создать страницу бота** (`/bot`)
6. **Добавить изображения** (заменить placeholder'ы)

## Полезные команды

```bash
# Разработка
npm run dev

# Сборка для продакшена
npm run build

# Запуск продакшен версии
npm start

# Проверка кода
npm run lint
```

## Структура проекта

```
├── app/              # Next.js App Router страницы
├── components/       # React компоненты
│   ├── layout/      # Header, Footer
│   ├── home/        # Компоненты главной страницы
│   └── ui/          # Переиспользуемые UI компоненты
├── lib/             # Утилиты и хелперы
│   └── supabase/    # Supabase клиенты
└── public/          # Статические файлы
```

## Дизайн-система

Все цвета, шрифты и spacing определены в `tailwind.config.ts` согласно PRD. Используйте классы Tailwind с кастомными цветами:

- `bg-primary-purple` - основной фиолетовый
- `bg-ocean-wave-start` - голубой (начало градиента)
- `text-deep-navy` - темно-синий для текста
- `bg-lavender-bg` - лавандовый фон

## Вопросы?

Смотрите подробную документацию в `website_prd.md`.



